# 契约变更说明 —— 20260911-sl398-min-segment-2000

> **状态:已批(统筹 2026-09-13;用户 2026-09-11 A20 / B23 / B25 裁定)。** 用户 2026-09-11 v5.6.13
> 回验 A20 原话「也许把最短段长上限调高到 2s 会好点」(统筹转达);本文档是该变更的契约面记录,
> 随实现 PR 落地并挂 `status/frozen-contract`。

## 变更了哪个冻结契约

- [ ] docs/PARAMETERS.md(自动化参数)—— **不动**(现树无值域句,真源 02 §0.3)
- [ ] docs/IPC_CONTRACT.md(共享内存段名/布局)—— 不动
- [ ] docs/STATE_SCHEMA.md(state schema)—— **不动**(现树无值域句,真源 02 §0.3)
- [ ] docs/SCVB_CONTRACT.md(桥面契约)—— **不动**(现树无值域句,真源 02 §0.3):
      §1.19 `setSegmentation` / §1.19 的载荷说明行(`:294` / `:498`)只有字段名与类型,
      **不增写值域**;字段名 / 类型 / 载荷形状一律不动
- [ ] tests/golden/(golden 快照)—— **已核:三份 golden 里没有 `500` 这个上界**
      (`params_v0.tsv` / `ipc-layout.txt` / `state/` 均无)⇒ 不动

> **值域真源不在本仓**:02-dsp-spec §0.3 的常量表(「`120 | 50..500 | state`」)在
> `masterPlan/plan/02-dsp-spec.md`,repo 里的引用全是注释与镜像。**改这一条要走 masterPlan + 修宪/
> 规格流程**,repo 内 `docs/constitution/params-v0.md` 是**只读副本**,只能从
> `masterPlan/constitution/` 同步(`scripts/check-constitution-sync.ps1` 断言 sha256)。

## 变更内容

**一句话**:`min_segment_ms` 的上限 **500 → 2000**(下限 50、默认 120 都不动)。

| 面 | 改前 | 改后 |
| --- | --- | --- |
| 规格值域(02 §0.3) | `120 \| 50..500` | `120 \| 50..2000` |
| native 夹取(`OutputEditor` 的 `jlimit`) | `juce::jlimit(50, 500, …)` | `juce::jlimit(50, 2000, …)` |
| UI 滑杆行程 | `min: 50, max: 500` | `min: 50, max: 2000` |
| 页面侧夹取 `clampMinSegMs` | `Math.min(500, Math.max(50, n))` | `Math.min(2000, Math.max(50, n))` |
| mock 快照值域 | 值域 50..500 | 同步到 50..2000(**mock 本身没有值域校验**,只查有限性 ⇒ 无落点,见下表) |
| VAD P1「丢短」 | `minHops = minSegmentMs / 10` ⇒ 上限 50 hops | 上限 **200 hops**(= 2s @10ms hop,即 100 hops/s × 2s);**本变更不新增上界**,但要确认段数/内存量级仍可接受 |
| 谷候选(`Segmentation.cpp`) | `minHops = llround(minSegmentMs / kHopMs)`、`minLen = msToSamples(...)` | 同上按新值域取值 |
| `restoreWindowTooShort` 口径 | 短段闸按夹取后的 `min_segment_ms` | **口径不变**(它读的就是夹取后的值);头注里的反例 **420 仍在 [50,2000] 内 ⇒ 无需重算**(该反例讲的是「毫秒判 vs 量化窗宽判」差两个 hop,与值域上限无关) |

**为什么要改**:MIN SEG 在真人声乐句上几乎无事可做(乐句 ≫500ms),上限抬到 2000ms 才让
「丢短段 / 并短段」在真素材上**可见**。用户 v5.6.13 回验 A20 报「变化很小、泳道图看不太出」,
SL-391 归甲(素材上短段本来就少)之后,这是让该参数真正可用的那一步。

## 兼容性影响

- **参数面**:ParamID / index / 顺序 / versionHint 全部不变,只是**允许的取值范围**变宽 ⇒
  既有工程的自动化数据、既有宿主会话全部照旧。
- **旧值 → 新版本**:**无旧值可读** —— 该值**不落盘**(见下一条),重开工程恒回默认 120ms,
  工程里根本没有「上次调过的值」这回事。
- **新值 → 旧版本**:**不存在这条降级路径** —— `segmentation.min_segment_ms` **今天不随工程保存**
  (只在 `OutputProcessor.h` 的 `runtime_.segmentationMinSegmentMs`;`OutputStateCodec` 的
  CFGS 不写 `segmentation.*`)。重开工程回到默认 120ms(分段三项参数的落盘另立 **SL-411**),
  所以旧版本读不到任何 >500ms 的值,也就无从「静默夹到 500」。
- **state**:字段类型仍是 `int`,编码不动,`abi` **不升**。
- **行为面(要写清)**:同样的素材在 50/120/500/1000/2000 五档下**段数应当单调不增**
  (丢短 / 并短更强)。这条是机检要钉的**方向**,不是「段数一定明显变少」——
  后者的前提是素材里真有 500–2000ms 的短段(见 house-rules §2 那次教训)。

## 要改的文件与行(只列,不改)

### 规格与文档(真源优先)

| 文件 | 位置 | 改什么 |
| --- | --- | --- |
| `masterPlan/plan/02-dsp-spec.md` | §0.3 常量表 | `min_segment_ms` 的 `50..500` → `50..2000`(**真源,走 masterPlan 流程**) |
| `docs/constitution/params-v0.md` | `:52` 一带的值域表 | **只读副本**,从 `masterPlan/constitution/` 同步,不就地编辑 |
| `docs/PARAMETERS.md` | `:54`(镜像行)与其中记值域处 | **无落点**(现树整份不带 `min_segment_ms` 值域句)⇒ 零改动 |
| `docs/STATE_SCHEMA.md` | `:28`(`segmentation: {mode, sensitivity, min_segment_ms}`) | **无落点**(该行不带值域注释)⇒ 零改动 |
| `docs/SCVB_CONTRACT.md` | `:294`(`setSegmentation` 参数行)、`:498`(`scvb.config` 载荷行) | **无落点**(两行只有字段名与类型,§1.19 **不增写**值域)⇒ 零改动;字段名/类型/载荷形状不动 |
| `docs/USER_GUIDE.zh-CN.md` + `docs/USER_GUIDE.md` | 分段参数说明段 | 值域句 + 版本兼容句;真源是 zh-CN,另一份同步改(硬约束那 7 处由生成器产出,本次未涉及) |
| `docs/ANALYSIS.md` | 若提到 MIN SEG 值域 | **无落点**(现树未提)⇒ 零改动 |

### 代码

| 文件 | 行 | 改什么 |
| --- | --- | --- |
| `src/output/OutputEditor.cpp` | `:1802`(`juce::jlimit(50, 500, …)`)与 `:1797` 的注释 | 上界 500 → 2000,注释同步 |
| `src/core/analysis/EnergyVad.h` | `:24`(`int minSegmentMs = 120;`) | 默认值**不变**;若注释写了值域则同步 |
| `src/core/analysis/EnergyVad.cpp` | `:237`(`minHops = p.minSegmentMs / 10`) | **不改代码**,但要在头注点名新上限(200 hops)与量级 |
| `src/core/analysis/Segmentation.h` | `:63` / `:122`(`minSegmentMs = 120.0`) | 默认值不变;注释里的值域同步 |
| `src/core/analysis/Segmentation.cpp` | `:275` / `:537` | 不改逻辑;确认 2000ms 下 `minHops` / `minLen` 无溢出与量级问题 |
| `web/output/tab-wave.js` | `:427`(滑杆定义 `min: 50, max: 500`)、`:455`(§0.3 引用注释) | 上界 → 2000 |
| `web/output/tab-wave.js` | `:305`(`clampMinSegMs` 的 `Math.min(500, …)`) | 上界 → 2000 |
| `web/output/tab-wave.js` | `:224` / `:231` / `:249`(`restoreWindowTooShort` 头注与反例 420)/ `:258` | **无落点**(头注无值域句;反例 420 与上限无关,本 PR 未动该头注)|
| `web/shared/mock-data.js` | `:557`(注释)、`:563`(值 420) | 注释同步;420 仍合法,可留 |
| `web-preview/mock/juce-bridge-mock.js` | `:1629`(值域校验) | **无落点**(现树**没有** `min_segment_ms` 值域校验,`setSegmentation` 只查有限性)⇒ 零改动;相邻那条 `[50,500]` 注释已同步为 `[50,2000]`(`:1034` 一带) |

### 机检 / 测试(改期望)

| 位置 | 现状 | 改什么 |
| --- | --- | --- |
| `web-preview/tests/smoke-tab3-interactions.mjs` | `:1033`(注释,「→ 50..500/120」)、`:4679`(「UI 行程 == native 夹取(50..500)」)、`:4734-4741`(`>= 50 && <= 500`,现改为**从 native 的 `jlimit` 派生上下界**,不再写字面量) | 三处上界 → 2000;注释里的来源句同步 |
| `tests/core/test_analysis_pipeline.cpp` | `:802`(`50/120/500 → 段数严格递减`) | 扩一档到 1000/2000,**保留单调不增**的方向断言;别写成「段数明显变少」 |
| `tests/host/test_host_harness.cpp` | `:7930` (`HOST SL391:改 min_segment_ms 重分析 → 段数随之变`) | 加一档 2000 的 host 接线格(证明真桥夹取到了 2000) |
| `docs/contract-changes/` | 本文件 | 随 PR 提交 |

### 明确**不动**的

- `tests/golden/**`(已核无 500 上界)。
- ParamID / index / 顺序 / versionHint / 默认值 120。
- `docs/constitution/**`(只读副本)。
- ADR-007(采集)与本变更无关。

## 审批

已批:统筹 2026-09-13(用户 2026-09-11 A20 裁定「也许把最短段长上限调高到 2s 会好点」);随实现 PR 落地并挂 `status/frozen-contract`。**值域真源不在本仓** —— `masterPlan/plan/02-dsp-spec.md` §0.3 那一行(`120 | 50..500`)由统筹在 masterPlan 侧同批改为 `120 | 50..2000`;`docs/constitution/params-v0.md` 是只读副本、不含值域,一律不就地编辑。
