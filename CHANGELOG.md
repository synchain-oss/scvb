# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

版本号唯一真源 = 顶层 `CMakeLists.txt` 的 `project(SCVB VERSION ...)`;发版流程与 tag 规则见 [docs/RELEASE.md](docs/RELEASE.md)。

固定小节 `### ⚠️ 契约变更` 置于其他小节之前,记录 state abi / IPC abi 升级、参数默认值变更、DSP 可闻变化与协议不变量变更;**没有内容时整节省略**,所以它一出现就是醒目的。每个 PR 自己改本文件,不留到发版时补写。

## [Unreleased]

> v0.1.0 是一个只能构建、不能用的空壳骨架。下面这一节因此是 SCVB 作为一件**产品**的第一份变更记录 —— 它描述的是从零到可用的全过程,而不是相对某个能用的旧版本的增量。**v0.1.0 从未打过 git tag、也没有发过 Release**(仓库公开后 [SL-73] 核实两个链接均 404 才发现),下面 `## [0.1.0]` 小节因此不再挂 Release 链接,`## [Unreleased]` 的比较区间也改成从首个 commit 起算(见文末链接)。

### ⚠️ 契约变更

- **VAD 五项与过渡时间随工程保存**(state 容器 abi 4→5,[SL-416],用户 v5.6.15 回验 A24):`analysis.vad.{threshold_db, hysteresis_db, hangover_ms, padding_pre_ms, padding_post_ms}` 与 `analysis.transition_ramp_ms` 此前只活在内存里 —— 存盘重开后五杆全回默认,**而分析确实按默认跑**(用户报的是「MIN SEG 回来了,但 THRESHOLD −45 dB / HYSTERESIS 3 dB / HOLD 200 ms / PAD PRE 120 ms / PAD POST 全部回默认」)。现在 `CFGS` 尾部再扩一整档 24 字节承载它们,容器 abi 4→5 + `migrate_4_to_5`(no-op;abi≤4 的旧工程整档缺席 ⇒ 回落规格默认 −38/6/250/120/200/80 且**不计回落**),golden 新增 `abi5.bin`(abi1–abi4 **保留**作迁移基线)。**连带(有意,报批)**:这六项的引擎初值此前是 −45/3/200(T29 遗留,与 02 §0.3 的出厂档、与 web 滑杆 `def` 都不一致),本版把**引擎初值 / 滑杆 def / 解码缺席与越界回落**三处收到同一处(§0.3 的 −38/6/250),四杆的行程也按 §0.3 收正(hysteresis `0..20`→`3..12`、hangover `0..500`→`100..600`、pad_pre `0..500`→`20..400`、pad_post `0..500`→`50..400`;threshold 与 ramp 一字未动),零回归(core / host 全量套件实跑;读数见 PR 描述)。详见 `docs/contract-changes/20260914-sl416-vad-persist.md`
- **分析结果现在可以撤销**(桥契约 §1.6 的「撤销」行由「否」改判为「是」,[J89] 2026-08-28 用户批准;变更文档 `docs/contract-changes/20260827-sl209-analyze-undoable.md`)。一次分析(全量 / 选区 / 单轨重新识别)= **一条**撤销步:Ctrl+Z 回到分析前的段表,Ctrl+Y 重放分析结果;与「重新分析不覆盖你手改过的段」天然共存 —— 撤销恢复的是「分析前的那一刻」,那一刻本就含着会被保留的手改段。此前分析是工具里唯一不可撤销的破坏性操作,不满意只能手工改回去(#152)
- **插件自有撤销栈的深度与内存改为显式定参**:预算 64 MiB、最少保 16 步。此前吃的是 `juce::UndoManager` 的默认值,配上按段数记账的新口径后,真实工程的撤销深度会固定在 30 步、而内存并没有上限(密集工程约 115 MB 常驻)。改后典型工程约 174 步可撤、密集工程约 17 步,内存有了真正的天花板。**用户可感知**:密集工程能回退的步数变少了,换来的是内存不再无限长(#152)
- viz 可视化段的**发布频率 4 Hz → 30 Hz**(用户拍板「好歹每秒保证 30Hz」)。段名 / abi / 布局 /
  字段语义一个字节未动,**不需要 abi+1、不需要段名升 v2**;读方按 seqlock 的 seq 消费,新旧版本
  互通不受影响。实测代价:写方 0.005% 一颗核、读方 0.023%
  (见 docs/contract-changes/20260827-viz-30hz.md 的实测表)(#115)
- 工程存档 abi 1 → 2:新增 analysis.loudness_mode / analysis.center_slot_policy 两字段的持久化,
  旧工程经 migrate_1_to_2 自动迁移,响度口径与中心槽策略回落各自默认值
  (见 docs/contract-changes/20260825-cfgs-persistence.md)(#81)
- **特征流不再自动转存外部文件**:「压缩后 >8MB 自动转 sidecar」加了**单点开关**,
  v1 出厂值 = **关** ⇒ 压缩后的特征流**一律内嵌**(`embedded=1`),不再产生
  `sessions/<GUID>/` 目录;把工程发给他人时,不会再有「特征体不在工程里」的坑。
  **只关写、不关读**:`embedded=0` + 合法引用的老工程照常载入,文件缺失照常明确报
  「特征文件缺失」。容器布局、FEAT 节编码、桥面载荷字段**零变化**(abi 不升、段名不升 v2);
  sidecar 的读写代码、8MB/6MB 回滞、事件与文案**全部保留** —— 开关是双向的,打开即逐字回到
  ADR-007 原文行为。**打开一份带 sidecar 的老工程并保存一次 ⇒ 特征收回内嵌、外部那份目录被回收**
  (`store.remove()`,**不可逆**,工程体积随之变大;这是开关关掉的直接后果,不是新增行为)
  (变更文档 `docs/contract-changes/20260911-sl395-sidecar-autoswitch-off.md`)(#258)
- **最短段长上限 500ms → 2000ms**(`segmentation.min_segment_ms`;下限 50ms、默认 120ms 不变,
  ParamID / index / 顺序 / versionHint 与 state 编码都不动,abi 不升)。**用户可感知**:长句素材上
  段数变少、每段更长,波形页 MIN SEG 滑杆的行程随之变宽。本卡落地时该值**不随工程保存**、
  重开工程回到默认 120ms —— **这一句已由 SL-411 取代**:自 abi=4 起分段三项随工程保存
  (见下面「分段三项参数随工程保存」那条);值域 50..2000 本身没有变
  (变更文档 `docs/contract-changes/20260911-sl398-min-segment-2000.md`)(#258)
- **分段三项参数随工程保存**(`analysis.segmentation.{mode, sensitivity, min_segment_ms}`;
  state 容器 abi 3→4,`CFGS` 尾扩一整档 12 字节,旧工程经 no-op `migrate_3_to_4` 自动迁移并取
  规格默认 valley / 50 / 120)。**用户可感知**:MIN SEG 拖到 1000ms、分析、存盘、重开工程,
  滑杆与读数还是 1000ms,**最短段长与灵敏度按它跑** —— 修复前一律跳回 120ms(而分析同样按 120 跑,
  于是「设置没生效」与「设置没被记住」在界面上长得一模一样)。
  ⚠ **分段方式(`mode`)在 v1 里是保留位**([SL-413] 定谳:界面上**从来没有**这个下拉 —— 不是这一版
  藏起来的;引擎也不消费它,`SegmentationParams` 里没有 `mode`。落盘只保证它不丢,不影响分析结果。
  详见变更文档 `docs/contract-changes/20260914-sl413-seg-mode-reserved.md`)。参数面零变化:三项都不是
  自动化参数,ParamID / index / 顺序 / versionHint 一个字节未动
  (变更文档 `docs/contract-changes/20260914-sl411-segmentation-persist.md`)(#259)
- **`analysis.segmentation.mode` 标注为 v1 保留位**(契约文本口径变更,**零布局 / 零 abi / 零迁移**:
  `CFGS` 尾部那一档里 `segmentationMode` 那一个 u32 照旧在(载荷偏移 `40 + languageBytes`,
  随 `uiLanguage` 长度浮动),容器 `abi` **本 PR 不升**(基线 `b06d37a` 上为 4;[SL-416] 已在 #263
  把它升到 5 —— 两处改动互不相交,本条一个字节都没碰 abi),`kMigrators` **本条不加**
  (现为四条,第四条的 `migrate_4_to_5` 来自 [SL-416]),
  `tests/golden/state/*.bin` 本 PR 一份未动)。`mode` 的两档 `valley` / `vad_only` 只活在规格里:
  **UI 全仓没有对应控件**
  (`web/output/index.html` 里 `valley` / `vad_only` / `分段方式` 三个字面全 0 命中;`valley` 在
  `tab-wave.js` 只作注释与契约 §1.19 整包缓存的初值),引擎侧 `SegmentationParams` 里也没有 `mode`
  (恒按 `valley` 走 S1),所以生产路径恒写 `0`=valley,`vad_only` 留待接线那张卡启用。
  **用户可感知**:界面上什么都不会变 —— 这一条是**把规格追认成事实**(用户 2026-09-14 裁 ②
  「把『分段方式』从 UI/规格摘掉」),不是新增一项行为;此前 CHANGELOG 与用户指南里那句
  「分段方式今天只回到原位」读起来像一个「你找得到、按了却没用」的控件,现已改成实话。
  ⚠ 「恒写 0」是**生产路径的实况,不是编解码的不变量**:`segModeOrdinal` / `segModeString`
  仍按原样往返(手写成 `1` 的工程读进来仍是 `vad_only`、存回去仍是 `1`)——
  契约 §7.3「不得静默丢数据」压过「统一成 0」。参数面零变化:123/124 个自动化参数一个字节未动
  (变更文档 `docs/contract-changes/20260914-sl413-seg-mode-reserved.md`)(#264)
- **你画的 pan 增益曲线现在真的会响**(DSP 可闻变化,[SL-442])。曲线编辑器里画的那条
  「pan 角度域增益曲线」此前**只画不响** —— 它进了分析(左右平衡按它算)、进了存盘、也画在
  界面上,唯独没有接进实时音频链;拖出一个 −12 dB 的深槽,听感上一点变化都没有。现在它按
  规格 02 §8.1 步骤 5 施加:增益查在**全局 width 缩放之后**的角度上,立体声轨的左右两个
  子声像**各查各的**。
  ⚠ **谁会听到变化**:**从没画过曲线的工程,声音一个字节都不会变**(空曲线 = 0 dB = 增益
  恰为 1,有逐位相等的用例钉住)。**画过曲线的工程声音会变** —— 变成你当初画的样子:这是补上
  一直没生效的效果,不是回归。
  改曲线或切版本时增益在 30 ms 内交叉淡入,不会咔哒(实测:单次提交造成的增益跳变上界 12 dB,
  最温和的手势——滚轮拧一格 Q——也有约 1 dB,没有「小到听不见」的档位)。
  ⚠ **已知缺口,本卡不修**:规格 §6.5 要求「改完曲线要提示重新平衡」,该提示**至今没有实现**
  (全局 width、每轨 width、participate_in_auto_pan 三个触发源同样没有)。后果是:改了曲线之后
  声音变了,而分析算出的左右平衡仍基于旧曲线、不再最优,而**界面上没有任何提示**。这个缺口在
  接上曲线之前就存在(width 早就是立即生效的),本卡只是多了一个触发源;已另行登记整族卡收口
  (#270)
- **桥面 `channel_id=0` 不再等于「未分配」**(契约文本追上已经发生的行为;**零布局 / 零 abi /
  零迁移 / 界面零变化**):`docs/SCVB_CONTRACT.md` 此前把 Input 首帧快照与 `scvb.state` 里的
  `channel_id=0` 定义成**等价于「未分配」**。这条等价自 #273 起不再成立 —— 顶层 `channel_id`
  报的是**你配置成了哪个通道**(你从没选过通道时它本来就是 `0`);**此外**只在「这次认领被拒、
  或共享内存段不可用」时才是 `0`,而那几态的 `claim` 分别是 `conflict` / `abiMismatch` /
  `idle`(**仅限其中「共享内存段不可用」那一支**),**都不是**「未分配」;反过来
  `claim="unassigned"` 时 `channel_id` 也未必是 `0`(会话已释放而你的配置原样留着)。
  **对照着这份契约写代码的人要注意**:判断「是不是未分配」一律以 `claim` 为准,不得由
  `channel_id=0` 反推。载荷字段、取值域、`claimState` 六值枚举与 `contractVersion` 全部未动,
  引擎与界面一个字节没改 —— 用户能感知的那部分行为变化记在上面 #273 那一条里
  (变更文档 `docs/contract-changes/20260921-sl464-channel-id-zero-semantics.md`)(#275)

### 新增

**两个插件**

- **SCVB Input(采集端)** —— 插在每条人声轨的最后一格。自动识别该轨是单声道还是立体声,按 A–H 组把音频与响度特征送给总线上的 Output;组被别的 Output 占用、采样率与总线不一致、或对面根本没有 Output 时,自动退回直通并在界面上说明是哪一种(#51、#54、#63)
- **SCVB Output(总线端)** —— 放在人声总线上接管一整组人声轨,分析、分段、自动分配声像与音量,再把结果写成宿主的自动化曲线。两个插件共用一个版本号,必须成对安装(#53、#55)

**分析与自动分配**

- 人声活动检测:按能量双阈值 + 回滞判断每条轨什么时候在唱,短促的误触发会被丢掉,句首句尾各留一段余量(#14)
- 自动分段:在能量谷处切分,支持排除区间与手工分段并存(#15)
- 段响度:BS.1770 K 加权积分为主口径,另可切 RMS 或峰值 dBFS 两档(#12、#13)
- 自动声像分配:匈牙利算法把各轨指派到声场槽位,再用 L/R 平衡迭代压平左右能量差;顶到边界时有四级回退,中心槽有「优先队列 / 主唱独占 / 均摊」三种策略可选(#16、#17)
- 声像曲线:bell / shelf / cut 三种形状与段间过渡,界面上画的曲线和 DSP 里跑的是同一条(#11)
- 局部重分析:只重跑「当前选区 + 选中的轨」,结果与已有分析拼接,不必整轨从头再来(#45)
- 分析结果的权威存储与增量拉取,Output 侧多轨并行取特征(#42)
- 上游改动的过期提示:在 Input 前面改了 EQ/压缩之后,Output 侧会指出「该轨上游音频与已采集
  特征不一致,建议重新采集」(横幅 + 波形页泳道 ⚠)。只提示,不阻断任何操作(#107)

**界面**

- Output 四页界面:总览、轨道、波形与分段、设置(#52、#60、#64、#62)
- 总览页分布图支持「分布 ↔ 轨迹」两种视图,轨迹视图按工程时间线画出每条轨的最终声像走向;配套 15 色轨道配色,颜色按色差与三型色觉模拟算出,不是挑出来的(#77)
- 声像曲线可视编辑器,浏览器侧与 C++ 侧的求值结果逐点对拍(#61)
- 波形与分段页:15 条泳道 + 段落详情与编辑闭环,支持滚轮缩放、拖拽平移与播放头跟随(#64)
- Input 单页:通道选择、组选择、远程视图与连接状态七态(#63)
- 首启引导:语言选择卡 → 九条硬约束红字 → 44 步全参数导览;设置页可随时重看(#65)
- 界面全量中文 / English / Français 三语(#65、#69、#76)
- 设置页新增「手动接管与自动化」说明块,讲清手动接管与自动化写入的关系(#70)
- 波形页滚轮补齐四路:裸滚轮左右平移、Shift 上下平移、Alt 改泳道高度、Ctrl 横向缩放
  (此前只有 Ctrl 一路)(#123)
- 波形页双击段与段之间那条分界线 = 删掉这条线、把左右两段并成一段(双击段内仍是在此
  分割,两者互为逆操作);连着微调同一条边界两次**不会**被当成双击而误删该边界
  (#123)
- 设置页的「文档」按钮现在真能点开了:按界面语言在系统浏览器里打开对应的用户手册
  (此前这颗钮从来没接过 handler)(#123)
- 轨道页与波形页的段落详情里新增常驻的「恢复自动」按钮:把某条轨从手动固定值交回
  引擎自动分配。此前这个操作**只有**解冻那一下会冒出来的临时提示条一个入口,点掉就
  再也找不到了。已锁定的段按约定不会被恢复,界面会直接说「请先解锁」而不是给一枚
  按了没反应的按钮(#148)
- Input 首启轻量引导:独立语言选择卡 + 5 步导览,页顶「?」可随时重看
  (见 docs/contract-changes/20260825-input-guide-seen.md)(#84)
- 总览页「分布 ↔ 轨迹」视图选择随工程保存(#80)

**版本、撤销与工程存档**

- 每个工程带两套平衡方案(V1 / V2),可整套复制、改名,改名可撤销(#48)
- 工程存档采用 TLV 容器 + 迁移框架,未知区块原样保留回写 —— 新版本写进去的字段,旧版本读到不会丢(#49)
- 分析特征可外挂 sidecar 存放:gzip 压缩、写时复制、8MB/6MB 回滞、SHA-256 校验,避免工程文件被特征数据撑大(#50)
- Output 页顶部常驻「撤销 / 重做」两枚按钮,与 Ctrl+Z / Ctrl+Shift+Z 共用同一个撤销栈(#86)

**自动化写入**

- 123 个自动化参数的完整布局冻结,并由 golden 测试钉死(#40)
- 打印层把分析结果经宿主自动化安全写出,带三层 host echo 屏蔽,避免自己写出去的值又被自己读回来(#47)
- FOLLOW / ARMED / PRINT 三态仲裁与统一平滑:跟随宿主、准备写入、正在写入三种状态下的取值来源与切换斜坡都有明确定义(#43)

**两插件之间的通道**

- 共享内存段布局冻结与启动校验(#36)、连接生命周期状态机(认领 / 接管 / 心跳 / 代际,#38)
- 双进程 IPC 契约测试台,26 条用例覆盖 IPC-1..20(#46)

**文档**

- 用户文档组:双语 README、[用户手册](docs/USER_GUIDE.zh-CN.md)(中英)、[发布流程与发布说明模板](docs/RELEASE.md)(#74)
- 九条硬约束红字的唯一生成器 `scripts/gen-hard-rules.mjs`:真源 = `docs/USER_GUIDE.zh-CN.md` 的 `## 硬约束` 小节,译文 = `docs/hard-rules.i18n.json`,机器生成其余 6 处落地面(USER_GUIDE EN、两份 README 快速上手、`web/shared/i18n.js` 的 zh/en/fr);`--check` 逐字节比对,任何位置手抄即红(#74)
- 双语文档结构对等检查 `scripts/check-doc-parity.ps1`,覆盖 README 与 USER_GUIDE 两对(#74)
- 契约文档组:[ARCHITECTURE](docs/ARCHITECTURE.md)、[ANALYSIS](docs/ANALYSIS.md)、14 份 ADR 拆分,以及三份冻结契约(参数总表 / IPC / state schema)转正入仓(#75)
- 协作文档组:CONTRIBUTING 全文、Contributor Covenant v2.1 原文与官方中译、SECURITY、第三方依赖声明、[新贡献者上手](docs/CONTRIBUTOR_ONBOARDING.md)(#73)
- [DAW 兼容矩阵与支持等级表](docs/DAW_COMPATIBILITY.md):Cubase / Studio One / Ableton Live / REAPER 四家 × 四类场景逐格有值,外加 16 条通用坑(#72)
- 桥契约 `docs/SCVB_CONTRACT.md`(#33)、端到端联调旅程与真机执行清单(#71)

**开发与验证设施**

- WebView2 装配层与「设计盒」唯一真源生成链 —— 界面尺寸只在一处定义,C++ 与 CSS 两侧机器对拍(#44)
- `web-preview/` mock 后端:不装 DAW 也能在浏览器里跑通全部界面状态,六套 fixture + 冒烟测试(#39、#37、#32)
- 比对工具 `scvb_nulltest`(逐声道残差、样本偏移、按位相等,#4)
- 合规件:gitleaks 密钥扫描、REUSE lint、SPDX 头检查(#5)
- 四个探路 spike 及其结论:S1 路由可靠性(#6)、S2 自动化写入(#20)、S3 WebView 承载力(#18)、S4 状态体量(#19)
- IPC 契约文档与 golden 布局的机器对拍,接入本地门禁 gate 3g(#85)
- **开窗诊断多记一个数:界面「画好了」比浏览器「首次绘制」晚多少**:排查开窗白闪时,我们
  此前只能从日志里看到「信号什么时候到、窗口什么时候亮」,**看不到页面究竟什么时候画出
  第一帧** —— 于是「这台机器上到底差多少毫秒」只能靠间接推算。现在这条差值由页面自己量好、
  随信号一起报上来,落在既有那行诊断的末尾:`(signal-firstPaint +N ms)`。N 就是这台机器上
  「报『画好了』− 浏览器首次绘制」的实测毫秒数。末尾也可能打的是 `(no paint record)`,
  那表示**这一次没有拿到首次绘制的读数**。⚠ **这两种都不用你自己判**:开窗看着不对劲时
  **把这份日志发回来**就够了。这一条只写日志,**不参与任何「什么时候把窗口亮出来」的判断**
  (#267)

### 变更

- **重分段之后那条摘要不再自称「有改动 0 段」**:那条摘要只在真跑过一次重分段之后才弹,却会印出「0 段有改动 · 新增 0 段 · 移除 0 段」—— 自相矛盾。现在三个计数**全为 0** 时改印「段数不变,声像/音量无变化」。⚠ 这句**只覆盖它说的这三项**:摘要能表达的是段数与声像/音量,**段边界挪动它表达不了**,所以没有写成「什么都没变」(#253)
- **冻结维度在写入自动化时改为打印一条平直线**(此前是完全不写)。冻结的含义是「这个值由我手动定死」,那它就应该以定值落进自动化曲线,写完后还能在 DAW 里自由手绘覆盖 —— 而不是留一段空白让宿主用别处的值填(#68)
- **冻结改为可逆的临时接管**:冻结期间调的值只存在参数面上,分析曲线不再被整表烘焙成一段定值 —— **解冻后该维度立刻回到引擎分析曲线继续运动**,重新分析也照常更新它。此前一旦冻结并调过值,解冻后声像就永远停在那个值上、重新分析也不动(#111)
- **冻结中调整不进插件的撤销栈**:那一路不改曲线、只写自动化参数,所以此时按 Ctrl+Z 撤掉的会是你更早的某一次段编辑,而不是刚才那下旋钮 —— 要回退冻结期间的调整,请用宿主自己的撤销(那些值是以自动化写入的形式落在 DAW 里的)。冻结维度上的手动写入也不再弹「将替换该轨全部分段结果」的确认条:它一段都不会替换(#111)
- `web/shared/i18n.js` 的 `guide.title` 与 `guide.rule1..9` 改为生成区,不再手写(#74)
- 界面文案第二轮定稿:「Armed」→「已就绪」、「打印中」→「自动化写入中」、「失联」→「链接异常」、「段检查器」→「段落详情与编辑」等(#76)
- CI 分三档跑,不再一刀切「所有 PR 都跑完整 CI」([J96] 用户新口径:只要确保**出包之前**
  跑过全量就行)。**轻档** = `feature/**` 的子 PR 且没碰 native 路径:只跑 format(clang-format /
  web-smoke / docs-truth)、compliance(gitleaks / REUSE / check-privacy / 设计盒)与三个
  review bot,省掉 20 分钟的 Windows 冷编译;**中档** = 里程碑 PR(base=`dev`)与 push→`dev`
  照跑全量;**重档** = **出包前硬门**,`workflow_dispatch` 必须跑一次并绿才允许打包 ——
  这是 `feature/v1` 主支线唯一的机器编译证据(#176)
- 轻档补回 native 编译覆盖:子 PR 只要碰了 `src/` `tests/` `cmake/` `CMakeLists.txt`
  **`web/`**(经 `juce_add_binary_data` 编进二进制)`third_party/` 或钉版文件,由 `detect-native`
  判定后**照跑全量**。[J96] 原来一刀切跳过的后果是「动了 C++ 的子 PR 在 CI 侧一次都不编译」,
  「CI 全绿」不再等价于「native 编过」;失效方向倒向「照编」,任何一步出错都不会倒向跳过(#180)
- S1 测试工程推子统一 −18 dB,避免 15 轨叠加时总线削波(#21)
- 代码评审机器人升级到 claude-opus-5(#67)
- Input 页顶部重排:语言选择挪到最右上角(与 Output 同位),「?」重看引导入口挪到卡片
  右下角,「组 {X}」标记落到第二行行首与品牌块左对齐;「已接管:本轨静音,声音由 Output
  总线输出」这句从折成两行改为**单行显示**,三语文案同批略作收窄(#177)
- 设置页「段落响度分析算法」的说明补上换档影响面:切换响度口径会重算各轨音量平衡;
  **声像通常不变,但素材差异大到平衡解需要重新指派时也会变化**;改后需重新分析。
  同一句话也写在改档弹框里。(初稿写的是「不改变声像分配」,实测站不住:首趟指派确实
  与响度口径无关,但换档会改平衡解收不收敛,落到回退链上就会重排槽位 —— 一组实测素材
  里 24 段变了 8 段。写死成「不变」等于对用户许一句做不到的承诺。)(#177)
- 建议表视图的标题区不再空一大块:标题、说明句与脚注此前各自带着浏览器默认的上下外边距,
  标题卡被撑到内容高度的两倍多。文案与字号一字未改(#177)
- 界面说明文案补回丢失的限定语与对应关系(三语),都是**读得出差别**的几处:Lead Select
  提示补回「是否参与音量调节为独立开关,不随此联动」(词条里此前整句丢失,照旧文案读会
  以为设 Lead 会连带改那一列开关);
  过渡时间提示补回「太短交界会跳,太长声像糊在两处之间」(这是调这个值的依据);宽度提示
  补回角度读数与自动化参数 `width 0–150%` 的对应;「01 采集」说明补回「这一步不写任何自动化」;
  MS 平衡提示补回「双击回 0」这个本来看不见的交互;写入确认条把「30 条**轨道**」改为
  「30 条**车道**」(只有 15 轨,车道才是 30 = 15×pan/vol),并补回「已 arm / 未 arm」与
  「仅试听、不落盘」的口径,全曲跟随变体同批统一措辞(#186)
- 轨道页表格下方那行常驻图例改得更好读:三列都补上列名(「参与音量调节」「参与自动声像」
  「冻结P/V」),句子由原先的「…计算和音量调节」这类拖尾改成「列名＝这一列是什么」;
  「冻结P/V」一段由「正常计入引擎计算,但不再被引擎驱动,旋钮解锁为纯手动控制」收紧为
  「结果照算但不再驱动,旋钮解锁为手动」。**极性与默认态一个字没改**——「音量」列仍是参与
  语义、仍标着「默认开」;变的是句式(原先「…音量平衡计算(默认开)和音量调节」这类拖尾,
  信息已折进列名)(#186)
- 波形页「重采集选区」由一次性按钮改为开关,形制与总览页「01 采集」同款:开着就一直
  看得见,并逐态说出现在到底在不在采 —— 已就绪·等待播放 / 重采集中 / 已离开重采集区 /
  需先打开 01 采集(最后一态尤其要紧:布防只标记要重录哪一块,真正让特征落盘的闸是
  01 采集,没开就一个 hop 都写不进去)(#114)
- 轨道页电平柱的起跳略作柔化:液柱与白色峰线改用「一个电平事件的间隔」(33ms)做补间,
  柱头照样够得到峰线,而起跳不再是硬邦邦的一跳(#123)
- 重采集那枚勾选框改叫「**区域外自动停止**」(原「播放结束自动停止」):自动撤防的判据
  本来就是播放头越过选区右边界,不是整段播完(#148)
- 工作流程卡的收口按钮从左下的「取消」改成右下的「确认」,并换成主按钮配色 ——
  原先那枚按钮在深色卡面上几乎与背景同色(#123)
- 响度口径与中心槽策略两项设置现在随工程保存,重开工程不再回到默认档(#81)
- 输出模式开关的 ON 档由「引擎驱动」改称「**写入自动化**」(OFF 档「跟随宿主」不变);
  页脚、加载守卫横幅、导览与工作流程卡里提到这个档位的说法一并同步(#123)
- **「sidecar(外置特征存储)」这一版不上了,相关界面一并收起**:用户 2026-09-14 裁定。设置页的「存储状态」行、顶部「采集数据缺失/过期」横幅、「已转存外部文件」提示三处都不再出现,首启导览里讲「存储状态」的那一步也整步移除(导览由 44 步变 43 步)。**收的是界面,不是能力**:外置存储的读写代码、以及「打开早期版本工程时把外部特征读回来」那条路一个字节没动,三语词条也全部保留;出厂态本来就是「特征恒内嵌在工程里」(见上面那条「>8MB 自动转存」的裁定)。**一处照实说**:那条「采集数据缺失/过期」横幅在 v1 本来就不会亮 —— 它等的那条 error code(`sidecarMissing`)在插件侧**没有生产者**(只有一条待接线注释),所以收起它没有夺走任何一条活着的提示;打开一份**外部特征文件已丢失**的早期工程时特征为空(分段与曲线不受影响),界面上没有解释这件事**在本次之前也一样**。将来接线发出该 code 时,必须连同横幅一并恢复。(#262)

### 修复

- **Input 右下角帮助钮改成深紫底**:上一版见 #260,把它从「浮在浅底上的一块白」改成了深墨底,问号是看清了,但那块近乎黑的底与整窗粉紫不同族,用户回验判「很丑」。这一版**只换底色值** —— 形状、尺寸、描边、白问号一字未动,底色换成用户指定的深紫 `#4a4060`,落在 tokens.css 里新增的 `--acc-chip-violet` 上(与提示卡那几枚紫**字**色各管各的角色)。可读性判据(问号 vs 钮底、钮底 vs 所在渐变面,两处 WCAG 对比度 ≥3:1)原样保留并照过(#266)
- **Input 右下角帮助钮不再是一块浮在浅底上的白**:改成深墨 chip(--acc-ink 底 + --txt-dark-1 问号,全是既有 token,没有新色值),smoke-ui-layout-page 新增的 A8 判据钉「问号 vs 钮面」「钮面 vs 所在底」两处 WCAG 对比度 ≥3:1,把旧的白玻璃样式注回即红(#260)
- **开窗占位有深浅了**:加载占位从单色 #d9cadb 升级为与成品外壳同形的渐变(tokens.css `--page-gradient` 同一组色标;C++ 色标数组 + 三份页面内联三处同源,改任何一处都会被 ⑥/⑥c 逮住),WebView2 那层只收纯色的底改取渐变中点色,FallbackPanel 面板底拆成自己的常量(#260)
- **点「恢复自动」之后,那一段的声像与音量与冻结前显示的不是同一对值**:冻结(或手动改)之前那一段显示 pan −20 / vol −0.2,点完「恢复自动」却变成 −60 / −0.9 —— 不是「没恢复」,而是**恢复出来的解不一样**。声像与音量不是逐段独立算的:引擎在一整条时间线上**逐段接力**解出来(每一段都受上一段的结果牵引,第一段还带着「你此刻的参数值」当锚),而这颗钮此前只把**选中那一段**交给引擎 —— 接力断在开头、锚又是你刚改过的值,解出来的不是全量分析对该段给出的那一个。现在它把**整条已采集时间线**交给引擎算(与「分析全部」同一道题面),**仍然只改你选中的那一段**:同轨其它段与别的轨一个字节都不动,人声活动绿线不在这一段上就不重画,操作照样一步 Ctrl+Z 退回。选区「重新识别」与段检查器的「恢复自动」(两颗钮走同一条「清除手动标记后重算」的路;「含手动段」只是工具条那颗的钮名)还有一处随之明确下来的行为:若全量分析在你选的这一段里**没有**切出任何段(这一段整段是静音),选区内**陈旧的自动段会被清掉** —— 此前这一发是「什么都不做」,与你按下的那颗钮写着的意思相反;选区落在一条更长的人声段里时,原段会被一条**裁到选区内的自动段**取代(所以可能变得比「最小分段」还短);**完全**在选区外的段、以及选区内**已锁定**的段照旧不受影响(跨着选区边界的自动段会被本次产出整段取代),手改过的段会被一并重算。工具条那颗「重新识别(含手动段)」的确认框会先告诉你要清几个;段检查器那颗「恢复自动」只作用在你选中的这一段上,确认框不给计数。代价是这一段重算的耗时与内存按整条时间线算(#257)
- **播放时那枚「宿主自动化正在写」的小图标要等按了停止才亮**:宿主在**起播**的一瞬间会把它当前所在位置的自动化值写一遍,而如果那个值与插件里的当前值相同,插件此前会认为「什么都没变」而**一帧都不发** —— 界面于是收不到「宿主正在写」这件事,只能等到播放停止、值真的变了才亮那一下(用户看到的就是「正常播放时图标不出现,停止那一下才闪 1 秒」)。现在只要**「宿主在写」这个状态本身翻了**,不管值有没有变都会通知界面,起播时图标立刻亮起(只要那个状态**翻了沿**就必发一帧;若宿主在**停走态**就已经连续在写、这个状态一次都没翻过,起播时不会多发一帧 —— 见 SL-409)、停止后照常熄灭。值没变时的去重仍然在:平直的那几秒不会刷屏,也不会漏掉起止那两次翻转(#257)
- **点了「重新分析 / 重新识别」有时什么反应都没有,现在会明说原因**:有两种情况插件会**拒绝**这次重算 —— ①你选的范围里根本没有采集到数据;②已经有一次分析在跑。此前这两种拒绝与「成功」在屏幕上一模一样:按钮照常亮着、点了什么都不发生、也没有任何提示,用户只能反复点或者以为卡死了。现在两种情况各给一句说明,并直接告诉你下一步该做什么(先采集这一段、或者换一个有数据的范围;或者等这次跑完再试)。波形与分段页的四颗钮(重分析选区、重新识别、应用、检查器里的「恢复自动」)与设置页那颗主钮走的是同一条判据(#256)
- **点「恢复自动」之后,那一段的声像回到正中、音量变成 0.0**:确认文案许诺的是「只重算选中的这一段」,该回到引擎算出来的自动值,结果回来的是一对零。原因不是哪一步失败了 —— **是我们只把一条轨交给引擎算**。声像是**指派**出来的:引擎把一个时间区间里同时在唱的几条轨分配到声场的不同槽位;而这颗钮只送了选中的那一条轨过去,引擎看见「这个区间里只有一个人在唱」,按设计就把他放到正中,左右也就没有需要平衡的差值,于是音量修正也是 0。它一直是**独唱该有的答案**,只是这颗钮给错了题面。现在重算仍然只**改**你选中的那一段,但会把这段时间里**所有采集到的轨**一起交给引擎当上下文,算出来的才是这一段在整首歌里本来该有的声像与音量;同范围内别的轨一个字节都不动。Tab2 的单轨「重新识别」是同一个毛病,一并修好(#255)
- **「恢复自动」不再顺手改掉别的轨的绿线**(与上一条同批修出来的):上一条让重算把这段时间里所有采集到的轨都当上下文算一遍,而**人声活动的绿线**(泳道上那条标示「这里在唱」的线)原先是**每条轨都跟着写一遍**的 —— 于是恢复一段会把别的轨的绿线一起重画。它尤其不该发生:绿线不在撤销的范围里,**Ctrl+Z 救不回来**。现在绿线也只写你选中的那一条轨。同批还收窄了「冻结」的清除面:恢复一段只解开你这一段所在轨的冻结,别的轨你锁着的照旧锁着(#255)
- **播放时那枚「宿主自动化正在写」的小图标有时会中途消失**:它此前靠「最近一次收到宿主写入之后的一小段保持期」点亮,而插件只在**参数值真的变了**的时候才收到通知 —— 自动化平直的那一段(比如一整句)根本没有通知进来,保持期一过图标就灭了,看着像随机掉线。现在改成:**只要这一次播放里观测到过宿主在写,图标就一直亮到你按停止**;停止后 1 秒内熄灭。轨道页上每轨控件的变暗跟着同一条判据走,两处不会再一个亮着一个灭了。**有一处要知道**:插件分不出「自动化是平的」和「自动化结束了」——两种情况传到界面上是同一件事(没有新的通知),所以自动化真的写完之后图标也会一直亮到停止。这是刻意选的:宁可多亮一会儿,也不要在播放中途消失(#254)
- **波形与分段页那排滑杆不齐:「最短段长」被撑成一整行**:上一版把「分段灵敏度」藏起来之后,那一排右边只剩「最短段长」一根,而排版还按两根分配空间,于是它独吞两份 —— 实测 228.6px,而左边五根各 106.3px。现在按「可见几根就分几份」记账,六根回到等宽(实测彼此相差 0.01px,只是分数像素的舍入)。纯排版修复(#253)
- **同一组里插了第二个 Output 之后,整个界面锁死、连组都换不了**:同组的第二个 Output 按设计是「只读观察」——它不抢主实例,界面上的写控件也确实该锁。问题出在**组选择器自己也被一起锁了**,而那是这一态**唯一的出口**:锁掉它等于告诉用户「你不能改这里,而改这里是你唯一能做的事」。现在只读观察态(以及宿主没给时间线那一态)下 GROUP 卡照常可点:选一个空组、确认,当场接管为主实例,红色横幅撤下、其余控件一起解锁;再切回被占的那一组也会如实变回只读观察。真正会锁住组选择器的只剩「正在写自动化」这一种,和以前一样。顺带把预览版的 mock 对齐真机 —— 此前 mock 一进只读观察就拒绝改组、连组号都不写,于是这一幕在预览里根本重现不出来(#252)
- **「分段灵敏度」滑杆暂时隐藏**(谷底切分尚未接入曲线,待 v1.1):这条杆拖了段数不会变 —— 谷底切出来的边界进不了分析产出的段表(段表另按「全局区间」成形),所以留着它就是一句做不到的承诺。**藏的是控件,不是数据**:工程存档里的字段、插件之间的协议、三语词条一个都没动,将来放回来不需要迁移。同一排的「**最短段长**」照旧可用,那一项本来就是好的(#251)
- **能量谷强制切分喂错了量纲,一次都没有切过**(内部修复,**这一版还看不到变化**):那一步收到的是**线性能量**,而它的判据是「谷要比两侧低多少 **dB** 才算一个切点」—— 两边差着量纲。线性能量算出来的落差在 0.01 上下,而判据的门槛最低也有 3.2,于是「这是个切点」在「分段灵敏度」整条滑杆上**恒不成立**。现在按规格先把能量包络换算成 dB 再送进去,同一段素材上灵敏度 5 / 50 / 95 三档分别切出 2 / 4 / 8 段。**但你这一版仍然看不出区别**:分析产出的段表另按「全局区间」成形,这一步新增的切点目前还进不到段表里 —— 那是另一件独立的事,尚未处理。所以这条只是把地基补正,「灵敏度滑杆有没有用」要等那件事一起才有答案。同批补了「最短段长」的判据:那一项**本来就是好的**(50 / 120 / 500 三档在同一素材上分别得到 4 / 3 / 1 段),此前只是没有用例钉住它(#251)
- **拖动 VAD / 分段滑杆,松手后什么也没发生**:界面上那条「300ms 后应用…」的进度条,等的是一个**从来没有被实现过**的动作 —— 松手之后本该自动重跑一遍分段,而插件里那一步是空的(界面刻意不自己计时,因为约定里这件事归插件做)。于是进度条走完就悄悄消失,段落一个都没变。现在这条流水线补上了:松手 300ms 后自动重新分段,**你手动改过或锁定的段一个字节都不会动**,并且**整次重分段算一步撤销** —— 不满意按 Ctrl+Z 就能退回去。顺带把结果摘要也补实了(此前无论改了多少,摘要恒显示「0 段」)。同批还理顺了三处**跟着这条路走**的表现:松手重分段之后**撤销按钮会正常亮起**(此前它一直灰着,而键盘 Ctrl+Z 却撤得动 —— 两个入口对不上);设置页的「参数已改、结果陈旧」提示**会正常消掉**(此前只认「点分析」那一种,松手重分段完它还挂着);以及**跑完发现段表其实一个字都没变时不再占一步撤销**(否则按一次 Ctrl+Z 看不到任何变化,却把攒着的「重做」历史吃掉了)(#166)
- **「分段」那两条滑杆调了不起作用**:三处都是界面自己那套数字与引擎规格对不上 —— ①「分段方式」的默认值引擎根本不认,于是开窗后第一次拖动**整包设置被拒**、三项一个都没进去(收到第一帧状态后自愈,所以只在刚开窗那一小段咬人);②「灵敏度」界面按 0~1 走、引擎按 0~100 收,于是 0.62 被当成「百分之 0.62」≈ 最低灵敏度,而引擎回推的值又会把把手顶到最右;③「最短段长」界面能拖到 1500ms,而引擎只收 50~500,上面三分之二是死行程。现在三处一律照引擎规格对齐(灵敏度 0~100、默认 50;最短段长 50~500、默认 120),预览版的 mock 也跟着同刻度(#164)
- **播放时「整体调整」页会变暗并开始闪烁**([J93]):变暗本身是设计 —— 它说的是「这条参数正被**宿主自动化**驱动,你在这儿改会被盖掉」(所以鼠标悬停有一句说明)。但判据看的是「最近一帧怎么说」,而那一位是每秒二十多次的采样量、且「值没变就不发帧」,于是它在「宿主还在写、我们的值恰好平着」时会误灭、下一拍又亮 —— 实测每张卡每秒翻转 1.3 次。现在改成**亮立刻、灭延迟**的闩锁:提示不再抖。同批按用户裁定还做了两件:**下面那张图表卡不再跟着变暗**(图表是只读展示,不存在「调了被盖掉」),三张参数卡的提示由**压暗改成一枚小徽标**(不再压暗、不挡操作,悬停说明保留)。轨道页每轨控件的灰显口径不变,但同一个判据是共享的,所以那边的抖动一并消失(#164)
- **插件窗口打开的一瞬间会闪一下全白**:开窗路径上三层都没有铺不透明底色 —— 编辑器组件没画背景、WebView2 底下那层是全透明的默认值、而 HTML 的底色写在两个**外链**样式表里,取回解析完之前那一帧根本没有底色,露出来的就是窗口的白。先给这三层都铺上底色(#164),但真机上仍看得见白与黑:铺的颜色与成品首屏不是一个明暗(那就是黑),而 WebView2 自己那个窗口在合成出第一帧之前画什么,插件侧根本没有 API 管得到(那就是白)。**最终的修法是根本不让它上屏**:导航开始到页面第一帧画完之间,WebView 被整块挪出可视区,那块地方由插件自己铺一层与成品外壳同色的占位;等页面确实画出第一帧、并且那一帧已经推上屏,才把它放回来。现在开窗看到的是「占位色 → 界面」,中间没有白也没有黑(#241、#247)
- **窗口四个角改回深色**:插件窗口的外壳是圆角的,圆角之外那一圈(以及外壳投影落的那个面)此前跟着开窗占位色一起变成了浅色。现在那一圈按设计稿取回深色,开窗时铺满窗口的占位色仍是浅色 —— 这是两件事,各自取各自的颜色(#247)
- **复制版本后切进去,总览页的声像分布图全轨居中**:复制版本按契约**一个参数都不写**,引擎打印头又只驱动当前激活的那一版,所以刚切进去、还没播放时参数面装的是出厂默认(声像居中);一播放打印头开始写,显示就全对了。轨道页早已改读曲线段(所以那边是对的),分布图却一直只读参数面。现在两处共用同一条读回优先级链,切进去当场显示的就是复制过来的曲线值。配套把 preview 的 mock 对齐真机(参数面按版本各存一份)—— 此前 mock 每次切版本都现造一帧带值的参数面,这一幕在预览里根本重现不出来(#159)
- **泳道上的绿线(有人在唱)播到哪消失到哪**:采集开着又把同一段放一遍时,每一个 hop 的旧判决都会被当成「素材换了」抹掉 —— 而重播写的正是同一批时间线 hop 号,于是播放头走过哪里,已分析段的绿线就被清到哪里,「有时有有时没」取决于分析完之后有没有再播过。现在只有该 hop 尚未被覆盖(新采到的地方,或清除覆盖后重采)、或正处在重采集布防期(用户明说这段素材要换)才作废旧判决;素材换了却没打洞也没布防的那一档:采集**关着**时换素材仍由「上游音频与已采集特征不一致」的 ⚠ 提示负责;采集**开着**时换素材(这时 ⚠ 不会亮)则要走「重采集布防」或「清除采集数据」,否则旧绿线会留到你下次重新分析为止 —— 换来的是最常见的那条路径(只是又听了一遍)不再掉线(#160)
- **采样率切换或宿主挂起后总线永久静音**:同一进程重新认领组时会被判为冲突,导致 Input 停止发送、Output 切直通(#23)
- **Cubase 崩溃链(共六轮实测收敛)**:共享内存环重映射后的悬挂指针(#26)、进程寿命泄漏导致宿主异步销毁实例时写向已解映射地址(#28)、单声道↔立体声重建时的竞态越界写(#29),最终改为环段恒按立体声容量创建并回填真实段大小(#30)
- **泳道波形卡在空白、只能靠切标签页救回来**:某一次取波形的回执没回来(宿主卡顿、分析在途销毁等),那个瓦片的键就一直占着「在途」不放,此后同一段时间轴再也不会重新取数 —— 一颗毒键毁掉整条泳道。现在单次取数 8 秒不回就把键腾出来,下一拍自动重发(#152)
- 界面导览的英文与法文译文审校必改 11 条(#69)
- DAW 兼容矩阵里的参数预算数字过期(93/94 → 123/124)与指向不存在条目的悬空引用(#78)
- `scvb_diag` / `scvb_nulltest` 在 GBK 控制台下中文乱码(#25);`scvb_nulltest` 与 `windows.h` 的 `min`/`max` 宏冲突导致 Release 编译失败(#27)
- IPC 契约测试的哈希断言偶发失败(段陈旧保活 + 判据本身时序依赖两类根因,#66)
- CI 上评审机器人 401、以及 Format 首跑失败(#7、#8)
- **分布图的柱子高度不再「都一样高」**:柱高一直绑着每轨音量,但映射曲线在归一之后**又除
  0.70** 再夹到 8%..88%,于是 **−1.82 dB 以上一律画成 88%** —— 而每轨音量的出厂默认就是
  0 dB,真实工程各轨也多坐在 0 dB 附近,结果就是齐刷刷一样高,上端 13.8 dB(38% 的值域)
  整段压平。现在 −24..+12 dB **线性铺满**块高、逐 dB 严格单调(0 dB → 61.33%)。
  同批新增一条**通栏的 0 dB 基准横线**(左端 `0 dB` 小标注),它与「0 dB 那根柱的顶边」
  同源,不是另写的一个数;卡片说明小字同步区分「短横线 = 立体声源张开度 / 通栏横线 =
  0 dB 基准」。**注**:该式自 Tab1 首次实现起就是这样,不是近期回归;三个月没被发现是因为
  预览用的示例轨最高只到 −1.7 dB,整张表都落在饱和点之下(#181)
- **改了响度口径之后,「需要重新分析」只有一枚小小的琥珀色标记,看不见**:现在改档会
  弹一次对话框说明分析口径已变,并直接给出「重新分析」按钮(点了就跑一次全量分析);
  另一枚按钮是「撤销更改」:点它会把刚改的那一项**改回上次分析用的那一档**,框关、
  琥珀标记同时熄灭 —— 你手上不会留下「我没决定、设置却已经改了」的中间态。想先放着
  不动就按 Esc(或点框外),那两条出口只关框、不改任何设置,琥珀标记继续留着提醒你
  口径和结果还对不上(这枚钮原本写着「稍后」、只关框,改动见 #244)。
  在别的档之间继续来回切,每换到一个新的脏值会再提醒一次;改回原来的档则自动收起。
  弹框**只在你自己改档时出现** —— 打开工程、切换版本、或以只读观察身份看别人的实例,
  都不会弹(那几种情况下仍然只有琥珀标记)。这是全工具里**唯一**一处弹出式对话框
  (#177)
- 分布图播放时每根柱子顶端往上拖出一条与轨同色的细竖线(一路到图顶,多轨同时):柱与
  张开线各自提成独立合成层,几何变化只重画自己那一层;张开半宽归零时连线粗一起归零,
  不再留下一小道痕(#178)
- **停播后「宿主自动化在写」徽标还挂着近两秒,而快速起停时它又会在播放中途消失**:两者
  是同一件事的两面 —— 按停那一刻熄灭计时还剩一大截,立刻重按播放,这一截残余就在新的一段
  播放里走完。熄灭延迟改为按走带态分两档(停走 0.9 秒 / 播放中 2.5 秒)(#178)
- **快速起停之后,「宿主自动化在写」徽标仍会在播放中途消失**——上一条只修掉了一半:
  停播后及时熄灭那一半是好的,这一半不是。判断该用哪一档熄灭延迟时,看的是**当前这一帧**
  的走带态,没有任何缓冲;于是播放途中只要来一帧「没在走带」(用户快速起停的那几十毫秒
  是一种,宿主某一小块音频给不出播放位置也是一种),延迟就当场从 2.5 秒缩到 0.9 秒,
  而播放中「距宿主上一次写超过 0.9 秒」本来就常见(2.5 秒那一档正是为它设的)——徽标
  应声熄灭。现在走带态本身也要**连续停满 0.5 秒**才算真停走,短促的那一下不再缩窗口。
  停播后的熄灭一个字节没动:0.5 秒短于 0.9 秒,最坏情况仍是停播后 0.9 秒内熄灭
- **开插件窗一闪而过的白屏**:此前铺的三层暗色没有一层管到真正在画白的那一段 —— JUCE 的
  WebView2 后端在浏览器控件建起来之前**每帧无条件填白**。现在在该层先调基类(白 + 控制器
  重试泵原样保留)再整块盖上暗底(#178)
- 重新分析后的改动摘要不再把「显示精度下根本看不出来的变化」列成一条,并按轨折叠;
  重新分析期间泳道保留旧帧,不再闪空(#179)
- **顶部那几条黄色提示横幅可以关掉了**:「上游音频与已采集特征不一致」「采集开着时不比对
  上游改动」「重采集布防还在但采集已关」三条各带一枚 ✕。关掉记的是**这一条提示当下说的
  那句话**,不是「以后都别提醒我」——过期轨数从 3 变成 5、或者条件消失之后又重新成立,
  提示都会再出来一次;关掉也只在本次插件会话内有效。剩下那几条红色/琥珀色横幅(路由失准、
  只读观察、采样率不一致、工程版本更新、采集数据缺失、宿主没有时间线)与加载守卫那条**没有**
  ✕:它们是「条件还在就得一直摆着」的横幅,条件解除时自己撤下(#244)
- **在循环区/手动范围下重新分析之后,设置页那枚琥珀标记不再叫你「重新分析」**:那种重算
  只覆盖你设定的范围,范围外的段仍是旧口径,所以标记本来就该继续亮着 —— 但它此前写的是
  「改后需重分析」,而你**刚刚就重新分析过了**。现在它改写「只更新了部分范围」;把范围切回
  全曲跟随、再分析一次,标记才消失。判定逻辑一个字节没动,只换了这一态下的说法(#244)
- 插件窗里右键不再冒出浏览器菜单(「重新加载」「查看网页源代码」之类);输入框上仍保留
  原生菜单,不然连粘贴都没了(#123)
- 「复制版本」确认框不再显示不全:它原先嵌在版本 chip 那个 210px 宽的容器里,蒙版只盖住
  那一小块,长警告文案当场被挤断;现移到卡片层并给了定宽(#123)
- 复制版本并切进新版本后、播放之前,15 轨声像不再齐刷刷显示居中:未冻结维度的读数此前
  一律回落到参数面,而参数面在没播放时装的是宿主的出厂默认。现在**按输出开关分档显示
  当下真正当家的那一方** —— 开关在「写入自动化」档时显示该版本曲线在播放头处的值
  (切进去就能看到这一版真正的起始声像),在「跟随宿主」档时仍显示参数面(那一档声音
  本来就跟宿主走)。冻结的维度两档都照旧显示参数面(#123)
- 切换版本的那一瞬间,15 轨声像/音量不再闪一下居中再跳回来:界面在插件把新版本的
  参数发过来**之前**就改按新版本去读,读了个空就回落成出厂默认。现在参数没到就继续
  显示切出时那一版的值,新值到了原子翻过去(#148)
- 按住 Alt 滚过滚轮之后,Ctrl+滚轮的横向缩放不再时灵时不灵:单独按一下 Alt 会激活
  Windows 的菜单模式、把焦点从界面上挪走,而 Alt+滚轮这个手势天然以松开 Alt 收尾
  (#148)
- **「重新识别(含手动段)」不再连上了锁的段一起抹掉**:约定里锁是第二道闸,「重新识别」
  只放开「这段是手动改过的」那一层保护,上了锁的段必须先逐段解锁才动得了 —— 插件侧此前
  把两层判据折在一起,于是锁形同虚设,用户特意锁住的段照样被重算结果覆盖。界面与
  web-preview 一直按约定行事,只有插件本体没有(#148)
- 轨道页解冻提示条的「重新识别(含手动段)」点了没反应 —— 这个请求按契约只给轨掩码、
  不给时间范围,而插件侧把缺省范围兜底成了 [0,0],分析当场被判成空范围拒掉(#112)
- **波形页「重分析选区」永远是灰的**:禁用判据错把「会被重画的已有段数」当成了「选区
  与已采集数据有没有交集」,而插件侧那个数从来没被算过、恒为 0 —— 于是真机上有选区、
  有数据时按钮也必然灰,影响预览行还一直报「将影响 0 区段」。刚采完还没分析过的素材
  更是首次分析永远点不动。同时:两件的禁用原因补成 tooltip,不再让人干瞪灰按钮;
  dry-run 的「手动编辑将保留」计数此前不按选区过滤,与事后实际保留数对不上,一并修正
  (#114)
- **轨道页的柱头够不着白色峰线**:液柱与峰线各自带一条 .18s 的 CSS 过渡,而电平事件
  每 33ms 就来一发 —— 柱头永远在追、永远差一截,唱到最响处也碰不到线。两条过渡一并
  去掉,液柱与峰线自此逐帧就等于弹道算出来的位置(弹道本来就由 JS 负责,上行也本来
  就是设计上的瞬时跟随)(#116;起跳的柔化随后由 #123 补上,见「变更」)
- Monitor 的声像/音量分布图不再一秒只跳一两下:发布端提到 30 Hz(契约变更,见上),读方一侧
  两处把数据面采慢的节流闸也一并修掉(4 Hz 采 4 Hz 周期性跳帧;250 ms 闸挂在 25 Hz 栅格上实得
  3.57 Hz),柱高/横位/张开度另由 rAF 在两帧之间线性补间(写方停摆即冻结,绝不外推)。
  端到端:页面实得 25 Hz,与 Output 侧同一张图同档(#115)
- 「上游改动 → 建议重新采集」的 ⚠ 又不见了([SL-225] v5.6 回归):点过「重采集选区」之后
  保存工程,布防**替你打开**的那一下 01 采集会被存进工程;而布防本身不随工程走,于是重开
  工程后采集莫名其妙开着、界面上没有任何线索 —— 而采集开着时 Input 一条指纹都不上报,那条
  ⚠ 从此再不出现。现在存的是**你自己选的**采集态,布防的临时接管不越过一次保存(#146)
- 「上游改动 → 建议重新采集」的 ⚠ **还是等不到**([SL-239] v5.6.2 实测,#146 之后):这次不是
  链断了 —— 那条比对**只在「01 采集」关着的时候**才做(采集开着时,这一秒的特征正在被写成新的
  基线,拿它跟自己比毫无意义),而流程里没有任何地方叫你把采集关掉。于是你改狠了上游 EQ、
  播一遍、什么都没等到,也看不出为什么;更糟的是那一遍**带着采集**的重播已经把基线刷成了改后
  的素材,事后再关掉采集也换不回那条提示了。现在采集开着且工程里已经有分段时,界面上会直说
  「采集开着时不比对上游改动 —— 要检查上游有没有动过,先关掉采集再播放」(#158)
- **「01 采集」不再随工程保存,重开工程一律是关的**([SL-247] / [J91],**行为变更,两面都说**):
  采集是一次**录制动作**,不是工程设置 —— 没有哪个 DAW 会把「录音已布防且正在滚动」存进
  工程再替你恢复。**好处**:以前莫名其妙开着的采集(v5.6 期间「重采集选区」替你打开的那一下
  被存进了工程)现在会自己好 —— 那批工程重开就是正常的,你什么都不用做;而采集开着时
  「上游改动 ⚠」整条是哑的,所以那条提醒也跟着回来了。**代价**:「重开工程后采集保持上次
  状态」这个行为**没有了**,每次要采集都得再点一次开关。契约变更文档
  `docs/contract-changes/20260830-j91-capture-not-persisted.md`(#162)
- **「01 采集」与「02 跟随引擎」两个开关现在互斥**([SL-247] / [J92a],用户拍板):手动打开
  其中一个,另一个自动关。**「重采集选区」是例外** —— 它替你打开采集时**不会**关掉你的
  输出引擎(否则点一个按钮就把引擎带走了,而撤防也不会把它还回来)。若你在布防期间手动
  打开跟随引擎,这次重采集会作废,界面上会明说「重采集布防还在,但采集已关」并给出出路
  —— 顺带补上一个一直没人管的老洞:布防期间你自己把采集关掉,以前界面上一个字都没有,
  只会觉得「布防着却什么都没采到」(#162)
- 两个插件在宿主里打开窗口即弹「加载超时」兜底面板 —— 界面资源从未编进插件二进制,
  外加看门狗预算过紧且把 WebView2 冷启动算进了页面加载窗口(#82)
- 轨道页电平表恒为地板值、循环区不显示、采集进度不更新、配置改动不广播 —— 四处数据面
  停在占位实现上从未接线(#87)
- 演示数据的轨道名未随界面语言本地化;导览蒙版下键盘 Tab 可以逃到背景并触发真实操作;
  Input 首帧在未测量声道数时误显示「已连接」与「MONO」(#79)
- 图表档位不再残留上一个工程的值 —— CFGS 缺失/损坏时仍按独立 UICF 块读取 master_chart_mode(#108)
- 切换图表视图后工程被正确标脏 —— setMasterChartMode 写入后补通知宿主(#108)
- 局部重采集布防终于真的收窄采集范围([J87]):点「重采集选区」会自动替你打开「01 采集」,
  此后只有 {勾选的轨} × {选区} 之内的特征会被改写,选区外与没勾的轨一个字节都不动;勾了
  「播放结束自动停止」时,播放头越过选区右边界即自动撤防,并把「01 采集」恢复成布防前的
  状态。此前 recaptureArm 的五个字段在引擎侧零消费方 —— 布防只把 badge 点亮,采集照旧按
  全局范围整条时间线重录,autoStop 连实现方都没有(#124)
- **整条时间线重新分析后,段表里不再有「短于 MIN SEG、且存在相接自动邻段」的自动段**:此前 MIN SEG 只在 VAD 核心与谷切分两层生效,而两轨的段交叠切出的独立区间会让段表出现比它短的段(v5.6.15 回验:MIN SEG=2000 下仍有 0.21s 的段)—— 现在回写层把短于它的自动段并入同轨**时间相接**的相邻段(优先前一段,值取被并入段;手动段不参与),区间图与指派零变化。**两侧都不相接的孤立短段按设计保留**,两条既有路:选区或范围档重分析在窗边裁出的残段,以及邻段因与手动段冲突而整条落选后剩下的孤段 —— 后者**整条时间线重新分析时同样会出现**(#261)
- **旧版本打开新版本存的工程时,现在会明说「请升级插件」,不再只给一份空工程**:工程存档里带一个
  递增的 `abi` 号,读到比自己高的版本时插件一直**正确拒载**(不静默降级、原样回写宿主那串字节,
  这部分从来没坏),但它**不告诉用户为什么** —— 提示那一半没接线,Release 里连一行日志都没有。
  用户看到的是段表、曲线、组号全空的工程,只会以为工程坏了。现在 Output 侧按契约 §2.9 发一条
  `newerState` 到那条红横幅④,上面写着「此工程由更新版本的 SCVB 保存,请升级插件(本机 abi {a} /
  工程 abi {b})」;条件成立就挂上,载入一份本机读得懂的工程后自动撤下(那条横幅按契约不给 ✕)。
  ⚠ **Input 侧同一通路仍未接线**(Input 的工程存档与 Output 共用同一个 `abi`,旧 Input 读新工程
  同样只有一行日志)—— 那一半不在本次改动面内(#264)
- **开窗那一段白:修掉了一条确实存在的成因**(Input / Monitor 上是否就此消失要真机回验;
  Output 一直就没有):开窗的正常次序是「浅紫占位 → 界面」,可 Input 与 Monitor 上占位之后总要再闪
  一下白。原因是插件判断「页面画好了没有」用错了信号:它等的是浏览器的两拍绘制回调,而
  这两拍**在页面还一个像素都没画的时候就会跑完**,于是窗口被提前亮出来,亮出来的是一块
  还没画上东西的空白。现在三页在**正常那条路**上都**等浏览器报出「首次绘制」之后**再报
  「画好了」,而且在那条路上这个先后次序是结构上成立的、不是靠调时间调出来的。
  浏览器里 12 轮实测:旧写法下 Input 这条
  信号比首次绘制**早 200~370 毫秒**,远大于插件放行前本来就要压的那 32 毫秒(⚠ 这个 32 毫秒
  已由下面的 SL-436 改成 64 毫秒,本条记的是 SL-429 当时的形态);Output 因为
  页面重、首次绘制本来就抢在信号前面,所以它从来不闪,不是 Output 做对了什么;而
  **Monitor 两者只差 ±10 毫秒上下 —— 按这套解释它本不该闪,可它稳定闪**,所以
  **Monitor 的白成因未明**。⚠ 在**真插件宿主**上又做了一次同机对照,那里的余量只有几十毫秒
  量级、不是几百 ⇒ **Input 的白也只能说「机制确实在,余量够不够致白没量到」**。这一版对三页
  都只保证「放行只会更晚、不会更早」,是否真的不闪要真机回验。代价是浅紫占位段变长
  **约 0.02~0.05 秒**(真插件宿主同机实测:Input +31 / Monitor +17 / Output +48 毫秒;
  浏览器里的数字大一个数量级,那是取样式表的网络往返,不作数)。⚠ **开窗仍可能出现那一下白**
  —— 这一版去掉的是已经查清的那一条成因,不是把它变成「绝不会出现」(#267)
- Input 通道被占用时点击冲突通道毫无反应:冲突反馈选择器构造错误导致 `querySelector`
  抛未捕获异常,卡片不抖、红 toast 不出、界面也不重绘(#269)
- Output 首启交互式导览开始后一段时间内仍显示用户真实工程数据,而旁边的说明词在讲解一份
  演示轨道(#269)
- Output 首启导览结束后,可能误清掉与导览无关、由别处逻辑独立置上的 `inert` 属性(#269)
- **开窗占位再稳一点**(调宽内部时序余量,不是新增机制,也不保证消除所有场景):首帧信号到达
  后放行前要再压住的毫秒下界从 32 ms 提到 64 ms —— 用户 2026-09-19 反馈 Monitor 页面开窗时
  白过一次,这一档把「信号到达」与「真正放回窗口」之间可以吸收的合成器延迟翻倍。代价上界从
  约 72 ms 涨到约 104 ms(占位段本身约 0.9 秒,涨的这一截感知不到)。bridge 插件同一处闸门
  已先落地同一个值(#271)
- **前端「首帧已绘」上行信号在极少数时序下会被吞掉、且不会重试**:三份 index.html 的信号函数
  此前把「撤掉 2.5 秒安全网」这一步写在了「原生通道是否就绪」的检查之前 —— 通道还没就绪的那
  一刻,网已经被撤,而消息其实从没发出去,安全网因此空转,插件只能吃满 3 秒兜底,本该有的提前
  放行机会白白错过。现在撤网挪到消息**真的发出去之后**:通道晚就位时安全网还在,2.5 秒到点
  会自动补发一次(#271)
- **焦点停在滑杆或勾选框上时按 Ctrl+Z,撤销到不了插件**(既有缺陷的修正,**用户可感知**):Output 的 Ctrl+Z 有一条「焦点在文本输入框里就不拦截」的豁免 —— 那里的撤销该还给正在打的字。但这条豁免此前只看元素**标签名**是不是 `INPUT`,而声像曲线工具条的 Q 滑杆、波形页的「自动停」勾选框、首启导览的「不再显示」勾选框在标签名上也都是 `INPUT`。于是**焦点一落在这三个控件上,Ctrl+Z 就被原样放给宿主的撤销栈**,插件这边什么都不会发生 —— 而滑杆和勾选框上根本没有「撤销刚打的字」这回事,豁免它们纯属误伤。现在改成按控件**类型**判:文本类输入框(含时间码、版本改名、轨道标签、段检视器那几个)照旧放行,滑杆 / 勾选框 / 下拉一律由插件接住。顺带两处一并收正:没写 `type` 的输入框按文本算(它本来就是文本框),显式标了「不可编辑」的区域不再被当成输入框放行(#274)
- **按住曲线上的点不放时按 Ctrl+Z,撤销会被松手那一下抹掉**(既有缺陷的修正):声像曲线编辑器在**按下**的那一刻就把整张点表抄了一份,松手时提交的是**那份抄本**。于是「按住 → Ctrl+Z →松手」会走成:撤销确实回到了上一笔编辑,紧接着那一记松手又把陈旧的抄本整表写了回去 ——刚撤销的那一笔当场复活。**此前几乎不可能察觉**:拖动期间声音本来就不会变,屏幕上也只是一条曲线回弹一下,看起来像手抖。现在撤销 / 重做会先**中止在飞的拖动**(放掉指针、丢掉那份抄本、停掉滑杆调 Q 与滚轮调 Q 那两条 140 毫秒的延迟提交),松手不再把那份陈旧抄本写回去(#274)
- **按住曲线上的点不放时切换版本,会把这一版的曲线整表写进另一版**(既有缺陷的修正,**会丢数据**):与上一条同源,但后果不同级 —— 上一条丢的是「刚撤销的那一笔」,这一条丢的是**另一个版本的整条曲线**。写入接口写的永远是「当前激活的那一版」、载荷里不带版本号,所以在 V1 上按住一个点不放、切到 V2、再松手,那份属于 V1 的点表会被整表写进 **V2**,V2 原本的曲线**当场被覆盖**(它不在这一步的撤销范围内)。现在切换版本同样会中止在飞的拖动,松手不再把 V1 的点表写进 V2。⚠ **有一档挡不住,写清楚**:切换版本在引擎侧是**立刻**生效的,而界面要等一拍才知道。假如恰好在这一拍之内有一笔**来自其它实例或自动化的**版本切换,而你手上又有一发刚发出去的提交,那一发仍可能落到切换后的版本上 —— 根因在写入接口本身不带版本号、界面没有办法指定目标版本;这一档留待接口改造时一并解决。**复制版本不受影响**:它写的是未激活的那一版、也不改「当前是哪一版」,在飞的拖动仍然属于同一版,松手照旧正确(#274)
- **点了一个已被占用的通道后,界面仍然显示"转移成功了"**(用户真机终验发现,SL-446/SL-19 复发):
  后端的认领其实被拒绝了(通道卡片抖动 + 红 toast 照常弹出),但**冲突发生前的那次选中态没有
  回滚**——用户如果没留意提示,会以为已经切到了新通道,实际音频仍在原通道上处理。现在冲突
  发生时会先尝试把原通道**抢回来**,大多数情况下界面会如实留在原通道(冲突提示照常弹出,
  只是选中态不再假装转移成功了);抢回也失败的极罕见情况下(原通道恰好在这一瞬被第三方抢
  走)才会如实显示"未分配"。⚠ 这次"抢回来"不是无损还原:抢回走的是同一条重新认领的路径,
  会让下游看到一次数据代际跳变(相当于这条通道重新连接了一次),不是悄无声息地接回原状。
  顺带修了一处更隐蔽的存档问题:**界面显示的"当前通道"与工程存档记的"配置的通道"此前共用
  同一个内部字段,两者其实是两件不同的事**——现在工程存档记的是"你配置成了哪个通道",
  与运行期这一刻是否真的连上无关;比如工程里存着通道 5、下次打开时 5 恰好被另一个实例
  占着,现在存档**不会**因为这次临时抢不到就被悄悄改写成"未分配"——你的配置原样留着;
  工程冲突提示上出现的通道号,也认的是你**这次实际点的那个号**,不会因为内部有一次"抢回
  旧通道"的动作就报错了号。（同一批里还确认过:不管是全新插件实例第一次打开工程,还是
  宿主复用一个已经在跑的实例去载入**另一份**工程(切换预设、复制轨道后重新载入),只要
  载入时撞了冲突,**工程文件里记的那个通道号都原样留着**——不会出现"打开一次工程,里面
  的通道配置就变了"这种情况。）
  ⚠ **界面顶部显示的"当前通道",这一批也跟着改了取值来源,一并写在这里**:绝大多数时候
  显示的是**你配置成了哪个通道**,不是"这一刻是否真的连上了"。这是为了修另一个此前没写
  进这里的问题——宿主做换音频设备、改缓冲区大小、冻结/禁用轨道这类会让插件短暂断开连接
  的常见操作时,界面此前会把这些操作误报成"未分配"(卡片选中态掉、首启空态引导重新弹出、
  优先级滑杆被禁),而工程配置其实根本没变。断开期间配置原样留着,下次宿主重新连接时
  (通常是下一次 prepareToPlay())会自动尝试用原来配置的号重新连上,连上后界面恢复正常,
  不需要用户手动做任何事。**唯独真正被拒绝的这一态例外**——点了一个当时正被占用的通道、
  又没有可以回滚回去的旧通道时,界面仍然如实显示"未分配",不会把被拒的号显示成已连接;
  这正是本条最初要防的问题,不因为上面那条改动而松口。(#273)
- **删掉或改组一条 Input 轨,会把同一工程里另一条 Input 轨从总线上悄悄踢下去**:那条轨的界面
  仍显示「已连接」,声音却不再出现在 Output 里,重开工程前恢复不了。成因是共享内存里的通道
  占用记录只按「哪个进程」认属主,而同一个 DAW 里所有 Input 插件都是同一个进程 —— 于是任何
  一条轨的释放动作都可能释放掉别人正在用的通道,甚至让第三条轨合法地占进同一个通道、两条轨
  同时往一个环里写。现在改成按「哪一个插件实例」认属主,并在释放成功的当下就交回属主身份
  (#277)
- **一条单声道轨的 Input 换成立体声轨的 Input、且沿用同一个通道号时,总线上会出现左右声道交替
  的撕裂噪音**(反过来立体声换单声道同样),界面的 ST/MONO 角标也停在旧值,要重载 Output 插件
  才恢复。成因是那块共享内存只要 Output 还挂着就不会消亡,后接手的 Input 直接沿用了前一条轨
  写下的声道数,两边按不同的声道数解读同一段音频。现在接手方会核对并改写声道数与采样率、
  给数据换代,Output 侧也会跟着刷新自己的解读方式(#277)
- **宿主不给时间线时,采集/输出开关看起来能开、实际什么都不做,也没有任何提示**:契约里写好的
  「宿主未提供时间线」琥珀横幅与两把开关的禁用态一直没有接上,于是打开采集会得到「成功」,
  却一帧特征都录不到。现在宿主连续 0.5 秒不给时间线,面板会亮出这条横幅,采集/输出开关与
  「重采集选区」一并拒绝,其余控件照常可用;时间线恢复或宿主停掉音频引擎后横幅自动撤下。单块的偶发缺失不会让横幅闪烁。
  ⚠ Cubase 恒提供时间线,这条在 Cubase 上不会出现、也无法在真机上复现,只经自动化测试验证;
  其他宿主或独立运行时才会触发。桥契约的返回值说明同步写全(#278)
- **分析跑着的时候切换版本,分析结果会整张写进切过去的那个版本**:在 V1 点「分析」,趁进度条
  还在走切到 V2,分析一完成,V2 原有的分段就被 V1 的结果替换掉,界面上只显示「分析完成」。
  现在分析进行中版本按钮是灰的(提示「分析中…」);万一切换请求还是到了引擎(比如界面和引擎
  不同步的那一拍),这一趟分析会被取消、结果整份丢弃,与点「取消」一样,两个版本的分段都不动。
  宿主载入工程或切换预设时,在途的分析同样作废:进度直接消失、没有任何提示,界面显示的是
  载入进来的那份工程自己的分段。拖动 VAD / 分段滑杆松手后自动重新分段的那几秒同样算「分析
  进行中」,版本按钮也会灰掉(#279)
- **点了「重新识别(含手动段)」再点取消,那些轨的冻结已经被悄悄解掉了**:声音从冻结值跳回
  引擎曲线,撤销也恢复不了。成因是冻结在分析开始之前就被清掉,而取消时什么都不还原。现在
  冻结要等这一趟分析真正完成、新的分段落地时才一起解除;被取消或中途被顶掉的分析,冻结一个
  都不动。分析进行期间,冻结的轨仍按原来的冻结值出声(#279)
- **写自动化(打印)刚开始的那一拍点版本按钮或「复制到…」,引擎会真的切过去**,打出来的
  自动化是两个版本拼起来的,事后没有任何提示。协议早就规定打印中插件本体必须拒绝这两个操作,
  但此前只有界面那一道闸,而界面判断「是否在打印」用的尺子与引擎不同,两者会差一拍。现在
  插件本体在打印中会拒绝切换和复制,界面随即把版本按钮灰掉并提示「自动化写入中不可切换版本」
  (#279)
- **实时路与宿主生命周期五处**:
  · 多条轨同时在响时,在面板上关掉其中一条,那一条现在按 80ms 淡出离开母线,不再在块边界上
    咔哒一声。所有轨都关掉时走的是总线整体交叉,这次没有改动。(#280)
  · 宿主一次给的块比准备时预告的大(常见于离线导出),Output 现在按预算分段把整块处理完,bypass
    状态下也一样。以前只处理每块的前一段,尾段原样留着宿主传进来的信号。⚠ 只修了 Output 侧;
    Input 侧采集的同类问题尚未修复,导出时的断音要等那一半一起分段才能消除。(#280)
  · 工程文件里段的声像或音量若是损坏值(非数字、无穷大或超出取值范围),整份段表拒载。已经开着的
    实例保留原来的段表;新打开这份工程时段表为空。以前会原样装进实时混音,母线可能整块变成 NaN
    或瞬间满刻度。⚠ 拒载后若保存工程,文件里原来的段表会被当前(可能为空的)段表覆盖,这是既有
    问题,本次没有修。(#280)
  · 44.1k 下采集的轨在 48k 下打开(或运行中改了采样率),该轨现在亮「数据过期,建议重新采集」
    提示。以前段落与自动化会整体错位约 8.8%,却没有任何提示。保存时特征仍记采集时的采样率。
    要撤下提示,先清除该轨的采集数据,清除时选中该轨,选区要盖住它全部已采集的范围,把这条轨清空;
    然后在当前采样率下重采。只清掉一部分,或不清除直接重采(包括局部重采集),提示都不会撤,因为
    没重采的部分仍会错位。工程里只记一个采集采样率:若只重采了部分轨,存盘后重开,已重采的轨也会再亮,
    需要把所有有数据的轨都重采。提示文案沿用上游改动那条,原因写的是「上游」。(#280)
  · 停用或冻结 Output 所在轨后,插件不再以最后一个播放头位置重开自动化 gesture。以前宿主在
    Write/Touch 档下,这段车道会被一个卡住的值占着。重新启用后照常打印。(#280)
- **调完音量或曲线的 Q 值后立刻按 Ctrl+Z 或切换版本,刚才那一下会撤错或丢失**:在轨道页用
  方向键 / 滚轮调音量、在声像曲线上拨 Q 滑杆或滚轮调 Q,这些改动要等你停手一小会儿
  (约 0.1–0.3 秒)才真正提交。此前在这段时间里按 Ctrl+Z:轨道页那一下还没进撤销栈,撤掉的是
  更早的一步,那一下随后才作为新操作记入、顺手清空了重做,撤错的那一步连 Ctrl+Shift+Z 都救
  不回来;曲线那一下则被直接丢掉,Q 值弹回去、更早那一步也被撤了。切换版本时,曲线那一下同样
  静默消失。现在撤销 / 重做 / 切换版本之前,会先把这些还在等待的改动**按原来的版本提交完**,
  Ctrl+Z 撤掉的就是刚才那一下;这一条取代上面「按住曲线上的点不放时按 Ctrl+Z」那条里「停掉
  滑杆调 Q 与滚轮调 Q 那两条 140 毫秒的延迟提交」的做法。按住不放的拖动(曲线上的点、轨道页
  的旋钮与音量卡箍、波形页段检查器的 PAN 旋钮与 VOL 滑杆)中途按 Ctrl+Z 或切换版本,这一次
  拖动仍然作废,松手不再提交(#282;曲线那一半同属 #282)
- **在波形页段检查器里拖 PAN 旋钮或 VOL 滑杆,松手后值偶尔没生效**:拖动期间只要任何一条轨
  的分段刷新了一下(播放或采集中会自动发生,不需要你做什么),拖好的值就在松手时被静默丢掉,
  旋钮弹回原值、没有任何提示。现在别的轨刷新不再影响正在调的这一段;同一条轨刷新时,正在拖的
  那个值保留,松手照常提交(#282)
- **波形页参数滑杆松手后,读数先弹回一下再跳到新值**:拖完 THRESHOLD 等滑杆一松手,数字会先
  闪回约一拍之前的旧值。现在松手后读数一直停在你放下的值,直到插件确认收到。顺带:用方向键调完
  一根滑杆、马上去调另一根,前一根的「调完」此前会被吞掉,之后在它上面按一次没有实际改动的
  方向键会白跑一遍重新分段;现在每根滑杆的键盘调节各自计时(#282;键盘那半 #282)
- **只读观察态下仍能拖动声像曲线,松手后无声弹回**:同组第二个 Output(只读观察)里,曲线上的
  点照样能拖、能双击加删、能用滚轮和方向键改,松手后又原样弹回,没有任何说明;双击版本按钮
  改名、点「复制到…」同样能操作到最后一步才无声失效。现在只读态下曲线不接受这些修改,选中点的
  工具条也不再出现;双击版本按钮不再进入改名,「复制到…」置灰(#282)
- **写自动化待命时点版本按钮,确认框开着去起一趟分析,回来点「继续」会把分析取消掉**:分析
  进行中版本按钮本来是灰的,但待命状态下先弹出的那个确认框不受这道限制。现在分析一开始,这个
  确认框就自动收起;分析结束后要切版本,请重新点一次版本按钮(#282)

<!-- ===========================================================================
待合并 PR 的预写条目。写在注释里而不是正文里:未合并的东西不该出现在渲染后的 CHANGELOG 上,
否则发版清单第 1 步「每条带 PR 号」检查不出它们其实还没进主线。

搬运规矩(SL-295 改成可执行口径;机检 = `scripts/check-changelog-drafts.mjs`):

① **谁搬、什么时候搬**:PR 作者自己,在**这个 PR 合并前的最后一次推送**里搬。不要留到合并
   之后 —— 合并之后没有人会再回来看这个块。SL-295 清点时块里积了 37 条已上线条目,最早的
   一条随 #107 上线,而正文里一个字都没有。
② **搬去哪**:本块的小节名与正文 `## [Unreleased]` 下的小节同名(⚠️ 契约变更 / 新增 /
   变更 / 修复),搬到同名小节的**末尾**。四个小节现在都已存在,不用新建;「新增」下面还
   分了粗体小组,挑语义最近的那一组末尾。
③ **改什么**:把 `(pending #<号>)` 换成 `(#<本 PR 号>)`,**条目正文一个字不改**。条目里若
   引用了同批别的卡(如「随后由 #SL204 补上」),那些引用**也要**换成真 PR 号 —— 渲染出去的
   CHANGELOG 不该出现只有仓内才认得的卡号或裁定号。**断行**可以重排(注释块里断在半途的路径,
   搬进正文会被渲染成中间带空格的样子),但只动换行位置,不动一个字。
④ **删什么**:本块里那一条**整条**删掉,不留副本。
⑤ **机检判什么**:`node scripts/check-changelog-drafts.mjs` 从 base 分支的提交标题里收集
   已上线的三种号 —— 卡号 `SL-<号>`(压缩写法 `SL-204/205/207` 按 `/` 展开成三个)、PR 号
   (标题**尾部**的 `(#<号>)`,或老式 `Merge pull request #<号> from`)、裁定号 `J<号>`;块里
   任何 `pending #SL<号>` / `pending #<号>` / `pending #J<号>` 落进对应集合就判红。PR 号只认
   落地位、不认标题里的引用 —— 否则一句「见 #83」就会把一个从未合并的 PR 判成已上线。
   它查得出「**合了却没搬**」,查不出「**搬了但内容不对**」—— 后者只能人工逐条比对合并提交
   (SL-295 那 37 条就是这么核的)。它判错了(标题只是顺带提了一句,那个号其实没有交付)走
   下面的「门禁放行」,**不要靠删条目躲开**。
⑥ 块尾「尚未开 PR 的在途卡」那几行没有 `pending #…` 标记,**不在判定面内**。
⑦ **正文也在判定面内**(SL-316 起,机检同一个脚本):搬上来之后,正文里每个**括号里的**
   `(#<号>)` 都必须在主线落过地,`#TBD` 一律判红。裸的「见 #83」不算引用、不进判定面。
   两种合法的放过写法:
   · **本 PR 自己的号** —— CI 透传 `SCVB_CHANGELOG_SELF_PR` 精确放过;本地没有这个变量时,
     凡是**大于当前最大落地号**的号只打 `[WARN]` 不判红(同批交叉引用「随后由 #123 补上」
     同理)。所以按规矩③ 写成 `(#<本 PR 号>)` 不会把自己判红。
   · **号确实对、机检判错了** —— 走下面的「正文放行」。判红的话术里会直接指到那一段,
     不用回来翻这里;**别改条目内容躲开**。

门禁放行(`- #<号> —— <理由>`,号写成与 `pending #…` 里一模一样的形态)
「号出现在合并标题里」不等于「它上线了」—— 例如 `32e52a3` 的标题里那句「SL-189 权威链定谳
(接线完好)」说的是查完不用改(SL-189 至今没有任何交付),机检却照样把它收进已上线集合。
这类假红在这里解除:一个号一行、**必须写理由**;放行会在每次运行时连号带理由打进输出,不做
静默豁免。条目搬进正文之后,要把对应的放行一起删掉(留着会判负)。写法不对(号没写成 `#SL189`
这种 token 形态、或破折号不是「 —— 」)同样判负并给出样例行,不会被静默忽略 —— 尤其别照抄上面
说明文字里 `SL-189` 那种**标题形态**。
- (当前没有需要放行的号)

正文放行(`- #<号> —— <理由>`,解析与上面「门禁放行」走同一份代码,但**号的形态不同**:
这一段**只吃纯数字 PR 号**)
上面那段管的是**块里**的预写条目;这一段管的是**正文里已经搬上来的号**。用在「这个号确实是
对的、机检却判红」的时候 —— 典型是主线用 **Rebase and merge** 合过一次(那种合并既不产生
merge commit、也不往 subject 里加 `(#N)`,那个号从此不出现在任何一条标题里,于是正文引用它
就成了**解除不掉的假红**)。同样一个号一行、**必须写理由**、每次运行连号带理由打进输出。
写法不对(号没写成 token 形态、破折号不是「 —— 」)同样判负并给出样例行。
⚠ **别照抄上面那段的 `#SL189`**:正文里的引用只可能是 PR 号,写成 SL 卡号 / J 裁定号
在这一段**放行不掉**;机检为此单独判负并说清真因(这道拦截若不在,它还会接着被当成
「已经不需要的放行」判负,而那句话术指的不是真因)。
⚠ 别拿它当「号写错了」的挡箭牌:号本身错(例如引了个从未合并的 PR)要去改号,不是放行。
- (当前没有需要放行的号)

⚠️ 契约变更

修复
- 选了中文后,切换页面或点「开始使用」语言变回英文;首启标记不落盘导致引导反复弹出(pending #83 —— **PR #83 已 CLOSED、从未合并**,主线没有任何 subject 带 `(#83)`;留在这里
  是对的,机检因此天然绿。它同时是 `check-changelog-drafts.mjs --self-test` 里「从未合并的号
  必须恒绿」那条用例的**真夹具** —— 要把它搬走或删掉之前,先另挑一个从未合并的 PR 号写进
  该脚本的 `FIXTURE_UNMERGED_PR` 并在本块里给它留一条预写条目,**别直接删断言**)

尚未开 PR 的在途卡(开 PR 时按上面 ②③ 的格式,先在本块里补一条预写条目)
- T41 建议表 + CSV 导出
- T44 可视化段 + Output 发布器
- T45 Monitor 插件壳
- T46 Monitor web UI
============================================================================ -->

## [0.1.0] - 2026-08-13

### 新增

- 首 commit:SCVB 仓库骨架(ADR-012 目录 + 开源必备八件套 + 空壳可构建双插件)。直接提交,无 PR:[`ae61f5f`](https://github.com/synchain-oss/scvb/commit/ae61f5f)

[Unreleased]: https://github.com/synchain-oss/scvb/compare/ae61f5f...HEAD
