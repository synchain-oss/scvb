# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

版本号唯一真源 = 顶层 `CMakeLists.txt` 的 `project(SCVB VERSION ...)`;发版流程与 tag 规则见 [docs/RELEASE.md](docs/RELEASE.md)。

固定小节 `### ⚠️ 契约变更` 置于其他小节之前,记录 state abi / IPC abi 升级、参数默认值变更、DSP 可闻变化与协议不变量变更;**没有内容时整节省略**,所以它一出现就是醒目的。每个 PR 自己改本文件,不留到发版时补写。

## [Unreleased]

> v0.1.0 是一个只能构建、不能用的空壳骨架。下面这一节因此是 SCVB 作为一件**产品**的第一份变更记录 —— 它描述的是从零到可用的全过程,而不是相对某个能用的旧版本的增量。

### ⚠️ 契约变更

- **分析结果现在可以撤销**(桥契约 §1.6 的「撤销」行由「否」改判为「是」,[J89] 2026-08-28 用户批准;变更文档 `docs/contract-changes/20260827-sl209-analyze-undoable.md`)。一次分析(全量 / 选区 / 单轨重新识别)= **一条**撤销步:Ctrl+Z 回到分析前的段表,Ctrl+Y 重放分析结果;与「重新分析不覆盖你手改过的段」天然共存 —— 撤销恢复的是「分析前的那一刻」,那一刻本就含着会被保留的手改段。此前分析是工具里唯一不可撤销的破坏性操作,不满意只能手工改回去(#152)
- **插件自有撤销栈的深度与内存改为显式定参**:预算 64 MiB、最少保 16 步。此前吃的是 `juce::UndoManager` 的默认值,配上按段数记账的新口径后,真实工程的撤销深度会固定在 30 步、而内存并没有上限(密集工程约 115 MB 常驻)。改后典型工程约 174 步可撤、密集工程约 17 步,内存有了真正的天花板。**用户可感知**:密集工程能回退的步数变少了,换来的是内存不再无限长(#152)

### 新增

**两个插件**

- **SCVB Input(采集端)** —— 插在每条人声轨的最后一格。自动识别该轨是单声道还是立体声,按 A–H 组把音频与响度特征送给总线上的 Output;组被别的 Output 占用、采样率与总线不一致、或对面根本没有 Output 时,自动退回直通并在界面上说明是哪一种(#51、#54、#63)
- **SCVB Output(总线端)** —— 放在人声总线上接管一整组人声轨,分析、分段、自动分配声像与音量,再把结果写成宿主的自动化曲线。两个插件共用一个版本号,必须成对安装(#53、#55)

**分析与自动分配**

- 人声活动检测:按能量双阈值 + 回滞判断每条轨什么时候在唱,短促的误触发会被丢掉,句首句尾各留一段余量(#14)
- 自动分段:在能量谷处切分,支持排除区间与手工分段并存(#15)
- 段响度:BS.1770 K 加权积分为主口径,另可切 RMS 或峰值 dBFS 两档(#12、#13)
- 自动声像分配:匈牙利算法把各轨指派到声场槽位,再用 L/R 平衡迭代压平左右能量差;顶到边界时有四级回退,中心槽有「优先队列 / 主唱独占 / 均摊」三种策略可选(#16、#17)
- 声像曲线:bell / shelf / cut 三种形状与段间过渡,界面上画的曲线和 DSP 里跑的是同一条(#11)
- 局部重分析:只重跑「当前选区 + 选中的轨」,结果与已有分析拼接,不必整轨从头再来(#45)
- 分析结果的权威存储与增量拉取,Output 侧多轨并行取特征(#42)

**界面**

- Output 四页界面:总览、轨道、波形与分段、设置(#52、#60、#64、#62)
- 总览页分布图支持「分布 ↔ 轨迹」两种视图,轨迹视图按工程时间线画出每条轨的最终声像走向;配套 15 色轨道配色,颜色按色差与三型色觉模拟算出,不是挑出来的(#77)
- 声像曲线可视编辑器,浏览器侧与 C++ 侧的求值结果逐点对拍(#61)
- 波形与分段页:15 条泳道 + 段落详情与编辑闭环,支持滚轮缩放、拖拽平移与播放头跟随(#64)
- Input 单页:通道选择、组选择、远程视图与连接状态七态(#63)
- 首启引导:语言选择卡 → 九条硬约束红字 → 44 步全参数导览;设置页可随时重看(#65)
- 界面全量中文 / English / Français 三语(#65、#69、#76)
- 设置页新增「手动接管与自动化」说明块,讲清手动接管与自动化写入的关系(#70)

**版本、撤销与工程存档**

- 每个工程带两套平衡方案(V1 / V2),可整套复制、改名,改名可撤销(#48)
- 工程存档采用 TLV 容器 + 迁移框架,未知区块原样保留回写 —— 新版本写进去的字段,旧版本读到不会丢(#49)
- 分析特征可外挂 sidecar 存放:gzip 压缩、写时复制、8MB/6MB 回滞、SHA-256 校验,避免工程文件被特征数据撑大(#50)

**自动化写入**

- 123 个自动化参数的完整布局冻结,并由 golden 测试钉死(#40)
- 打印层把分析结果经宿主自动化安全写出,带三层 host echo 屏蔽,避免自己写出去的值又被自己读回来(#47)
- FOLLOW / ARMED / PRINT 三态仲裁与统一平滑:跟随宿主、准备写入、正在写入三种状态下的取值来源与切换斜坡都有明确定义(#43)

**两插件之间的通道**

- 共享内存段布局冻结与启动校验(#36)、连接生命周期状态机(认领 / 接管 / 心跳 / 代际,#38)
- 双进程 IPC 契约测试台,26 条用例覆盖 IPC-1..20(#46)

**文档**

- 用户文档组:双语 README、[用户手册](docs/USER_GUIDE.zh-CN.md)(中英)、[发布流程与发布说明模板](docs/RELEASE.md)(#74)
- 九条硬约束红字的唯一生成器 `scripts/gen-hard-rules.mjs`:真源 = `docs/USER_GUIDE.zh-CN.md` 的 `## 硬约束` 小节,译文 = `docs/hard-rules.i18n.json`,机器生成其余 6 处落地面(USER_GUIDE EN、两份 README 快速上手、`web/shared/i18n.js` 的 zh/en/fr);`--check` 逐字节比对,任何位置手抄即红(#74)
- 双语文档结构对等检查 `scripts/check-doc-parity.ps1`,覆盖 README 与 USER_GUIDE 两对(#74)
- 契约文档组:[ARCHITECTURE](docs/ARCHITECTURE.md)、[ANALYSIS](docs/ANALYSIS.md)、14 份 ADR 拆分,以及三份冻结契约(参数总表 / IPC / state schema)转正入仓(#75)
- 协作文档组:CONTRIBUTING 全文、Contributor Covenant v2.1 原文与官方中译、SECURITY、第三方依赖声明、[新贡献者上手](docs/CONTRIBUTOR_ONBOARDING.md)(#73)
- [DAW 兼容矩阵与支持等级表](docs/DAW_COMPATIBILITY.md):Cubase / Studio One / Ableton Live / REAPER 四家 × 四类场景逐格有值,外加 16 条通用坑(#72)
- 桥契约 `docs/SCVB_CONTRACT.md`(#33)、端到端联调旅程与真机执行清单(#71)

**开发与验证设施**

- WebView2 装配层与「设计盒」唯一真源生成链 —— 界面尺寸只在一处定义,C++ 与 CSS 两侧机器对拍(#44)
- `web-preview/` mock 后端:不装 DAW 也能在浏览器里跑通全部界面状态,六套 fixture + 冒烟测试(#39、#37、#32)
- 比对工具 `scvb_nulltest`(逐声道残差、样本偏移、按位相等,#4)
- 合规件:gitleaks 密钥扫描、REUSE lint、SPDX 头检查(#5)
- 四个探路 spike 及其结论:S1 路由可靠性(#6)、S2 自动化写入(#20)、S3 WebView 承载力(#18)、S4 状态体量(#19)

### 变更

- **冻结维度在写入自动化时改为打印一条平直线**(此前是完全不写)。冻结的含义是「这个值由我手动定死」,那它就应该以定值落进自动化曲线,写完后还能在 DAW 里自由手绘覆盖 —— 而不是留一段空白让宿主用别处的值填(#68)
- **冻结改为可逆的临时接管**:冻结期间调的值只存在参数面上,分析曲线不再被整表烘焙成一段定值 —— **解冻后该维度立刻回到引擎分析曲线继续运动**,重新分析也照常更新它。此前一旦冻结并调过值,解冻后声像就永远停在那个值上、重新分析也不动(#106)
- **冻结中调整不进插件的撤销栈**:那一路不改曲线、只写自动化参数,所以此时按 Ctrl+Z 撤掉的会是你更早的某一次段编辑,而不是刚才那下旋钮 —— 要回退冻结期间的调整,请用宿主自己的撤销(那些值是以自动化写入的形式落在 DAW 里的)。冻结维度上的手动写入也不再弹「将替换该轨全部分段结果」的确认条:它一段都不会替换(#106)
- `web/shared/i18n.js` 的 `guide.title` 与 `guide.rule1..9` 改为生成区,不再手写(#74)
- 界面文案第二轮定稿:「Armed」→「已就绪」、「打印中」→「自动化写入中」、「失联」→「链接异常」、「段检查器」→「段落详情与编辑」等(#76)
- 所有并入 `dev` 的 PR(含 `feature/v1` 的子 PR)现在都跑完整 CI(#56)
- S1 测试工程推子统一 −18 dB,避免 15 轨叠加时总线削波(#21)
- 代码评审机器人升级到 claude-opus-5(#67)

### 修复

- **采样率切换或宿主挂起后总线永久静音**:同一进程重新认领组时会被判为冲突,导致 Input 停止发送、Output 切直通(#23)
- **Cubase 崩溃链(共六轮实测收敛)**:共享内存环重映射后的悬挂指针(#26)、进程寿命泄漏导致宿主异步销毁实例时写向已解映射地址(#28)、单声道↔立体声重建时的竞态越界写(#29),最终改为环段恒按立体声容量创建并回填真实段大小(#30)
- **泳道波形卡在空白、只能靠切标签页救回来**:某一次取波形的回执没回来(宿主卡顿、分析在途销毁等),那个瓦片的键就一直占着「在途」不放,此后同一段时间轴再也不会重新取数 —— 一颗毒键毁掉整条泳道。现在单次取数 8 秒不回就把键腾出来,下一拍自动重发(#152)
- 界面导览的英文与法文译文审校必改 11 条(#69)
- DAW 兼容矩阵里的参数预算数字过期(93/94 → 123/124)与指向不存在条目的悬空引用(#78)
- `scvb_diag` / `scvb_nulltest` 在 GBK 控制台下中文乱码(#25);`scvb_nulltest` 与 `windows.h` 的 `min`/`max` 宏冲突导致 Release 编译失败(#27)
- IPC 契约测试的哈希断言偶发失败(段陈旧保活 + 判据本身时序依赖两类根因,#66)
- CI 上评审机器人 401、以及 Format 首跑失败(#7、#8)

<!-- ===========================================================================
待合并 PR 的预写条目 —— 各 PR 合并时把自己那条移到上面对应小节并去掉本注释里的副本。
写在注释里而不是正文里:未合并的东西不该出现在渲染后的 CHANGELOG 上,否则发版清单第 1 步
「每条带 PR 号」检查不出它们其实还没进主线。

⚠️ 契约变更(合并时需新建 `### ⚠️ 契约变更` 小节,置于「新增」之前)
- viz 可视化段的**发布频率 4 Hz → 30 Hz**(用户拍板「好歹每秒保证 30Hz」)。段名 / abi / 布局 /
  字段语义一个字节未动,**不需要 abi+1、不需要段名升 v2**;读方按 seqlock 的 seq 消费,新旧版本
  互通不受影响。实测代价:写方 0.005% 一颗核、读方 0.023%(见 docs/contract-changes/
  20260827-viz-30hz.md 的实测表)(pending #SL192)
- 工程存档 abi 1 → 2:新增 analysis.loudness_mode / analysis.center_slot_policy 两字段的持久化,
  旧工程经 migrate_1_to_2 自动迁移,响度口径与中心槽策略回落各自默认值
  (见 docs/contract-changes/20260825-cfgs-persistence.md)(pending #81)

新增
- Output 页顶部常驻「撤销 / 重做」两枚按钮,与 Ctrl+Z / Ctrl+Shift+Z 共用同一个撤销栈(pending #86)
- Input 首启轻量引导:独立语言选择卡 + 5 步导览,页顶「?」可随时重看
  (见 docs/contract-changes/20260825-input-guide-seen.md)(pending #84)
- 总览页「分布 ↔ 轨迹」视图选择随工程保存(pending #80)
- IPC 契约文档与 golden 布局的机器对拍,接入本地门禁 gate 3g(pending #85)
- 上游改动的过期提示:在 Input 前面改了 EQ/压缩之后,Output 侧会指出「该轨上游音频与已采集
  特征不一致,建议重新采集」(横幅 + 波形页泳道 ⚠)。只提示,不阻断任何操作(pending #SL177)
- 波形页滚轮补齐四路:裸滚轮左右平移、Shift 上下平移、Alt 改泳道高度、Ctrl 横向缩放
  (此前只有 Ctrl 一路)(pending #SL205)
- 波形页双击段与段之间那条分界线 = 删掉这条线、把左右两段并成一段(双击段内仍是在此
  分割,两者互为逆操作);连着微调同一条边界两次**不会**被当成双击而误删该边界
  (pending #SL207)
- 设置页的「文档」按钮现在真能点开了:按界面语言在系统浏览器里打开对应的用户手册
  (此前这颗钮从来没接过 handler)(pending #SL214)
- 轨道页与波形页的段落详情里新增常驻的「恢复自动」按钮:把某条轨从手动固定值交回
  引擎自动分配。此前这个操作**只有**解冻那一下会冒出来的临时提示条一个入口,点掉就
  再也找不到了。已锁定的段按约定不会被恢复,界面会直接说「请先解锁」而不是给一枚
  按了没反应的按钮(pending #SL230)

变更
- 响度口径与中心槽策略两项设置现在随工程保存,重开工程不再回到默认档(pending #81)
- 波形页「重采集选区」由一次性按钮改为开关,形制与总览页「01 采集」同款:开着就一直
  看得见,并逐态说出现在到底在不在采 —— 已就绪·等待播放 / 重采集中 / 已离开重采集区 /
  需先打开 01 采集(最后一态尤其要紧:布防只标记要重录哪一块,真正让特征落盘的闸是
  01 采集,没开就一个 hop 都写不进去)(pending #SL193)
- 轨道页电平柱的起跳略作柔化:液柱与白色峰线改用「一个电平事件的间隔」(33ms)做补间,
  柱头照样够得到峰线,而起跳不再是硬邦邦的一跳(pending #SL204)
- 输出模式开关的 ON 档由「引擎驱动」改称「**写入自动化**」(OFF 档「跟随宿主」不变);
  页脚、加载守卫横幅、导览与工作流程卡里提到这个档位的说法一并同步(pending #J88)
- 重采集那枚勾选框改叫「**区域外自动停止**」(原「播放结束自动停止」):自动撤防的判据
  本来就是播放头越过选区右边界,不是整段播完(pending #SL228)
- 工作流程卡的收口按钮从左下的「取消」改成右下的「确认」,并换成主按钮配色 ——
  原先那枚按钮在深色卡面上几乎与背景同色(pending #SL213)

修复
- 两个插件在宿主里打开窗口即弹「加载超时」兜底面板 —— 界面资源从未编进插件二进制,
  外加看门狗预算过紧且把 WebView2 冷启动算进了页面加载窗口(pending #82)
- 选了中文后,切换页面或点「开始使用」语言变回英文;首启标记不落盘导致引导反复弹出(pending #83)
- 轨道页电平表恒为地板值、循环区不显示、采集进度不更新、配置改动不广播 —— 四处数据面
  停在占位实现上从未接线(pending #87)
- 演示数据的轨道名未随界面语言本地化;导览蒙版下键盘 Tab 可以逃到背景并触发真实操作;
  Input 首帧在未测量声道数时误显示「已连接」与「MONO」(pending #79)
- 图表档位不再残留上一个工程的值 —— CFGS 缺失/损坏时仍按独立 UICF 块读取 master_chart_mode(pending #108)
- 切换图表视图后工程被正确标脏 —— setMasterChartMode 写入后补通知宿主(pending #108)
- 插件窗里右键不再冒出浏览器菜单(「重新加载」「查看网页源代码」之类);输入框上仍保留
  原生菜单,不然连粘贴都没了(pending #SL207)
- 「复制版本」确认框不再显示不全:它原先嵌在版本 chip 那个 210px 宽的容器里,蒙版只盖住
  那一小块,长警告文案当场被挤断;现移到卡片层并给了定宽(pending #SL211)
- 复制版本并切进新版本后、播放之前,15 轨声像不再齐刷刷显示居中:未冻结维度的读数此前
  一律回落到参数面,而参数面在没播放时装的是宿主的出厂默认。现在**按输出开关分档显示
  当下真正当家的那一方** —— 开关在「写入自动化」档时显示该版本曲线在播放头处的值
  (切进去就能看到这一版真正的起始声像),在「跟随宿主」档时仍显示参数面(那一档声音
  本来就跟宿主走)。冻结的维度两档都照旧显示参数面(pending #SL211)
- 切换版本的那一瞬间,15 轨声像/音量不再闪一下居中再跳回来:界面在插件把新版本的
  参数发过来**之前**就改按新版本去读,读了个空就回落成出厂默认。现在参数没到就继续
  显示切出时那一版的值,新值到了原子翻过去(pending #SL229)
- 按住 Alt 滚过滚轮之后,Ctrl+滚轮的横向缩放不再时灵时不灵:单独按一下 Alt 会激活
  Windows 的菜单模式、把焦点从界面上挪走,而 Alt+滚轮这个手势天然以松开 Alt 收尾
  (pending #SL227)
- **「重新识别(含手动段)」不再连上了锁的段一起抹掉**:约定里锁是第二道闸,「重新识别」
  只放开「这段是手动改过的」那一层保护,上了锁的段必须先逐段解锁才动得了 —— 插件侧此前
  把两层判据折在一起,于是锁形同虚设,用户特意锁住的段照样被重算结果覆盖。界面与
  web-preview 一直按约定行事,只有插件本体没有(pending #SL230)
- 轨道页解冻提示条的「重新识别(含手动段)」点了没反应 —— 这个请求按契约只给轨掩码、
  不给时间范围,而插件侧把缺省范围兜底成了 [0,0],分析当场被判成空范围拒掉(pending #SL190)
- **波形页「重分析选区」永远是灰的**:禁用判据错把「会被重画的已有段数」当成了「选区
  与已采集数据有没有交集」,而插件侧那个数从来没被算过、恒为 0 —— 于是真机上有选区、
  有数据时按钮也必然灰,影响预览行还一直报「将影响 0 区段」。刚采完还没分析过的素材
  更是首次分析永远点不动。同时:两件的禁用原因补成 tooltip,不再让人干瞪灰按钮;
  dry-run 的「手动编辑将保留」计数此前不按选区过滤,与事后实际保留数对不上,一并修正
  (pending #SL193)
- **轨道页的柱头够不着白色峰线**:液柱与峰线各自带一条 .18s 的 CSS 过渡,而电平事件
  每 33ms 就来一发 —— 柱头永远在追、永远差一截,唱到最响处也碰不到线。两条过渡一并
  去掉,液柱与峰线自此逐帧就等于弹道算出来的位置(弹道本来就由 JS 负责,上行也本来
  就是设计上的瞬时跟随)(pending #SL191;起跳的柔化随后由 #SL204 补上,见「变更」)
- Monitor 的声像/音量分布图不再一秒只跳一两下:发布端提到 30 Hz(契约变更,见上),读方一侧
  两处把数据面采慢的节流闸也一并修掉(4 Hz 采 4 Hz 周期性跳帧;250 ms 闸挂在 25 Hz 栅格上实得
  3.57 Hz),柱高/横位/张开度另由 rAF 在两帧之间线性补间(写方停摆即冻结,绝不外推)。
  端到端:页面实得 25 Hz,与 Output 侧同一张图同档(pending #SL192)
- 局部重采集布防终于真的收窄采集范围([J87]):点「重采集选区」会自动替你打开「01 采集」,
  此后只有 {勾选的轨} × {选区} 之内的特征会被改写,选区外与没勾的轨一个字节都不动;勾了
  「播放结束自动停止」时,播放头越过选区右边界即自动撤防,并把「01 采集」恢复成布防前的
  状态。此前 recaptureArm 的五个字段在引擎侧零消费方 —— 布防只把 badge 点亮,采集照旧按
  全局范围整条时间线重录,autoStop 连实现方都没有(pending #J87)
- 「上游改动 → 建议重新采集」的 ⚠ 又不见了([SL-225] v5.6 回归):点过「重采集选区」之后
  保存工程,布防**替你打开**的那一下 01 采集会被存进工程;而布防本身不随工程走,于是重开
  工程后采集莫名其妙开着、界面上没有任何线索 —— 而采集开着时 Input 一条指纹都不上报,那条
  ⚠ 从此再不出现。现在存的是**你自己选的**采集态,布防的临时接管不越过一次保存(pending #SL225)
- 「上游改动 → 建议重新采集」的 ⚠ **还是等不到**([SL-239] v5.6.2 实测,#146 之后):这次不是
  链断了 —— 那条比对**只在「01 采集」关着的时候**才做(采集开着时,这一秒的特征正在被写成新的
  基线,拿它跟自己比毫无意义),而流程里没有任何地方叫你把采集关掉。于是你改狠了上游 EQ、
  播一遍、什么都没等到,也看不出为什么;更糟的是那一遍**带着采集**的重播已经把基线刷成了改后
  的素材,事后再关掉采集也换不回那条提示了。现在采集开着且工程里已经有分段时,界面上会直说
  「采集开着时不比对上游改动 —— 要检查上游有没有动过,先关掉采集再播放」(pending #SL239)

尚未开 PR 的在途卡(合并时按上面格式补条目)
- T41 建议表 + CSV 导出
- T44 可视化段 + Output 发布器
- T45 Monitor 插件壳
- T46 Monitor web UI
============================================================================ -->

## [0.1.0] - 2026-08-13

### 新增

- 首 commit:SCVB 仓库骨架(ADR-012 目录 + 开源必备八件套 + 空壳可构建双插件)。直接提交,无 PR:[`ae61f5f`](https://github.com/synchain-oss/scvb/commit/ae61f5f)

[Unreleased]: https://github.com/synchain-oss/scvb/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/synchain-oss/scvb/releases/tag/v0.1.0
